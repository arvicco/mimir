# runtime data -- never commit
data/
scripts/lppl/data/
scripts/scenario/data/
scripts/btco/capstruct/pending/
scripts/btco/capstruct/state.json
scripts/btco/universe.json.bak-*
*.status

# btco: capstruct/<TICKER>.jsonl ledgers and universe.json ARE committed
# (audit trail); test fixtures are recorded inputs; other *.jsonl are runtime
*.jsonl
!scripts/btco/capstruct/*.jsonl
!test/fixtures/**/*.jsonl

# secrets
.env
*.token

# local working notes -- never commit
.docs/

# OS noise
.DS_Store

# editor
*.swp

# claude code -- machine-local settings (a shared settings.json IS committed)
.claude/settings.local.json
wrangler.generated.toml
