# Worklog

One line per completed packet: date · packet · commit · notes.

2026-08-11 · Gate-9 review fixes · -- · Two owner-caught regressions from the M9-13/15 tooltip work: (1) applyTerms merged {legend:{tooltip}} into legend-less charts, CREATING a default-shown phantom legend that collided with the scenario title -- now wires only where the option already shows a legend; (2) the lppl_shadow formatter block (light-on-dark text) rendered on ECharts' default white tooltip, unreadable -- now carries the house dark chrome. Both pinned by web regression tests; verified by screenshot + real hover.

2026-08-11 · M9-16 · (with M9-15 push) · Spot line added to the BTC GEX profile (parity with MSTR, owner request at the Gate 9 preview); label at the bottom line-end after the top-position first attempt collided with flip -- caught in self-review screenshots. MSTR banding untouched.

2026-08-11 · M9-15 · de968c0 · Glossary hover explanations via a sixth additive renderer hook, meta.terms = { TERM => plain explanation } (owner Gate-9 ruling). Wired in render.js to the fitting surface: drawn ECharts legends (legend.tooltip + styled formatter -- ATM IV/RR25/FLY25, spread/MSTR/BTC, 7d..90d, spot/flip/CW/PW), the scenario module scoreboard (canvas axis labels -> triggerEvent + a viewport-fixed styled popover), and the gex_cp venue widget (per-venue CSS bubble). Owner-approved texts verbatim. terms rides meta only -> no golden drift (asserted); 27 written/0 skip, key count unchanged. Playwright drove CW/RR25/macro/DERI hovers (all render the lppl_shadow-style block, all in-viewport, console clean). Skill file records the hook in the same commit.
2026-08-11 · M9-14 · (with M9-13 push) · Same-day repair for the vol_spread daily history row -- owner caught the null Aug-11 point at the Gate 9 preview; root cause: the 00:48Z first tick had a transient Deribit failure and the once-per-day date guard froze it. Later runs now replace today's row when strictly more spreads are non-null; never downgrades; atomic rewrite. Live row heals at the first post-deploy tick.

2026-08-11 · M9-13 · (phase-9) · Gate-9 preview feedback (owner ruling 2026-08-11): (1) the M9-11 shadow scoreboard must NOT eat the LPPL panels' right margin -- the three panels get their full width back; (2) every shadow number needs a genuinely good hover explanation ("right now it's incomprehensible mess of random numbers"). lppl_regime reverted to its byte-identical pre-M9-11 three-panel form (golden re-blessed == 41dc59a^) and given tab_group 'lppl' (LPPL, tab_pos 0 = default). New chart:lppl_shadow (fn :lppl_shadow, tab_group 'lppl' / SHADOW / tab_pos 1) renders the six frozen-vs-shadow checks as full-width readable rows -- stat (bold), frozen, amber ->, shadow, one-phrase verdict (rivals win / wins at 2y / not met / still wins / still noise / drift flatters) -- all visible WITHOUT hover, on a category y-axis with label-only scatter columns at fixed x. Per-row hover: new 'lppl_shadow' registry formatter in render.js (precedent gex_levels) shows the bold stat, the "frozen .. -> shadow ..  <verdict>" line, then the owner-approved plain-language paragraph, carried VERBATIM in the option (LPPL_SHADOW_EXPLAIN const, stashed on the row's stat-column datum so the renderer stays dumb and payloads stay JSON). Axis-trigger tooltip fires anywhere on a row; the universal never-clip position callback keeps the 320px block inside the viewport -- Playwright bottom-viewport check (raw mouse.move) confirms the freeze row's tooltip flips ABOVE the pointer, TOOLTIP_IN_VIEWPORT=true, and DEFAULT_TAB=LPPL (default unchanged). Bookkeeping: 26->27 keys everywhere (pipeline expected count, index.html PRODUCERS + PUB LIVE string, CSP sha256 refresh, Gate-9-runbook EXPECT lines + KV arithmetic), CARD_ORDER gains chart:lppl_shadow after chart:lppl_regime, fixture reused (already carries shadow fields). Goldens: chart_lppl_regime re-blessed (byte-identical to pre-M9-11) + new chart_lppl_shadow, both PROVISIONAL; no other golden diffs. Full rake green (742 runs, 0 failures); PUBLISH_DRY_RUN=1 publish -> 27 written + index, 0 skip; screenshot-reviewed at 1440px (LPPL tab full-width panels, SHADOW tab six readable rows, hover block legible + confined). Not yet pushed -- CI is the 3.3 authority.

2026-08-11 · M9-12 · (phase-9) · Production runtime separation -- agents and the deploy-time publish now run from an APP-MANAGED clone, not the dev checkout, so ~/Dev/mimir becomes an ordinary git folder whose branch never affects production (Opus impl, Fable spec). BTC::Env gains pure path helpers live_dir = ~/Library/Application Support/mimir/live and data_home = .../data (home injectable). rake deploy (lib/btc/deploy.rb) gains a live-runtime sync BEFORE wrangler/publish: REFUSES unless the deployed HEAD is pushed to origin (git branch -r --contains, no network), then brings the clone onto that exact sha -- clone from the local repo when absent (repointing origin at the real remote URL) else fetch + checkout --detach; the deploy's own publish then runs FROM the clone (sh -c cd LIVE, spaced path shell-escaped) with BTC_DATA_DIR=data_home; DEPLOY_DRY_RUN prints the plan and mutates nothing. The three wrappers (run_publish/gex_snapshot/suite_history.sh) default BTC_DATA_DIR to the data home after sourcing the env file, honoring an explicit override. ops:install (lib/btc/ops.rb) renders the launchd plists with the program line pointing at the live clone, adds a pre-flight `live runtime` row (fails with "run rake deploy first" if absent), reads snapshot/status verification from the data home, and runs a one-time interactive data migration: it prints a source->destination->file-count inventory for every data_dir suite (lppl, scenario, gex_history, vol_history, vol_spread, source_cache -- the full grep-verified set; capstruct is a committed audit trail and /tmp status files are ephemeral, both deliberately excluded) then copies dev-tree data into data_home/<suite> with skip-if-destination-newer (idempotent). Migration planner is a pure function, unit-tested with tmpdirs; deploy sync tested with an injectable git-runner (absent-clone / present-clone / unpushed-refusal / failed-step / dry-run sequences); ops Fake harness extended with a live clone + program-root. Grep inventory finding: NO ops/*.rb producer bypasses the BTC::Env.data_dir seam (suite_history shells the suite scripts cwd-relative from LIVE; publish_health uses /tmp by design), so no producer code changed. Dry migration plan against the real ~/Dev/mimir dev tree: 74 files across the six suites, zero writes. Docs: README ops section rewritten to the live-runtime model (3 agents; btco-alert retired), Gate-9 runbook deploy EXPECT gains the `live runtime -> <sha7>` line + a new step 3a (rake ops:install: migration inventory table, three agents on the new paths, kickstart PASS) with an honest data-continuity note, RUNBOOK/DEPLOY.md + the plist install comments updated. Full rake green each commit (compat + health + tests); local ruby 4.0.x is NEWER than the 3.3 CI target -- CI is the 3.3 authority (not yet pushed). Deploy EXECUTION + the launchd install/migration remain HUMAN actions (Golden Rule 3).

2026-08-11 · OWNER RULINGS: worktree discipline · -- · Two rulings after the owner found ~/Dev/mimir-phase8 still present a month past its merge: (1) worktrees OUTSIDE the repo folder are a banned anti-pattern -- they live at .worktrees/<branch> (gitignored) inside the checkout; (2) a worktree whose branch has merged is removed at the gate that merged it, never left to rot. Enforcement: BTC::Health.scan_worktrees joins the rake health gate (pure check_worktrees unit-tested; anchors on git worktree list's first entry so the check works from any worktree; flags outside-repo AND stale-merged). Executed immediately: mimir-phase8 worktree removed, mimir-phase9 moved to .worktrees/phase-9, Gate-9 runbook paths updated + a remove-the-worktree step added to its merge section, CLAUDE.md convention + phase line fixed.

2026-08-11 · M9-11 · 41dc59a · Surface the Phase-9 shadow numbers on the LPPL card so the D9 rulings can be made with frozen-vs-shadow in view (Opus impl, Fable spec + review, mimir-design bound). The lppl_regime card gains a compact "shadow" scoreboard top-right (side-panels-right ruling): a 4th grid, row names on a category y-axis (right-aligned in the gutter, exactly the scenario module-strip mechanism), values as per-datum labels on a silent, invisible (symbolSize 0) scatter kept out of the axisPointer crosshair. Six rows, all flowing ADDITIVELY through the lppl:latest payload publish already carries into chart:lppl_regime, read DEFENSIVELY -- a missing shadow field drops its ROW (fail-soft, no null drawn), and a payload with NO shadow fields degrades the card byte-for-byte to its pre-M9-11 three-panel form (right margin back to 24, whole-card axisPointer link): mean/eval = sum of per_horizon.{30,90,180}.mean_per_eval (D9-b; real -1.26 = M9-1's documented density-honest headline, = total differential/n_evals), 365/730 = per_horizon_long means (D9-c; +0.16 at 730d), damping = fit.damping vs damping_ref_threshold with a <1/≥1 annotation (D9-e), impr = rmse_impr_pct→improvement_v2 (D9-e), p(osc) = p_value→p_value_v2 (D9-f), freeze = envelope.bound→freeze_candidate (D9-g). Numbers scaled/compacted at build time per the design system (leading-zero strip so a p reads .38 not 0.38; a → arrow for frozen→shadow, the glyph the scenario drift already uses). Card meta help gains one sentence per row naming its decision item; the rare trough note moved from right→left to clear the new column. Rows cluster as a compact block top-right under a "shadow" header (density beats airy; also avoids any row reading as an annotation of the panel beside it) rather than spreading full height. No verdict/panel/score content changed. Fixture payload_lppl_latest.json extended additively with realistic, internally-consistent synthetic shadow values (per_horizon sums match the frozen bf_by_horizon; synthetic addition noted in payloads/README.md). Golden chart_lppl_regime.json re-blessed PROVISIONAL after a 1440px headless screenshot read against the design skill (compact block, no clipping, no collision with the ratio pin or the panels, arrows + compact numbers render); NO other golden diffed. Additive contract tests added (scoreboard rows/values, fail-soft row drop, no-shadow degrade-to-three-panels, leading-zero strip) + two existing lppl structure tests scoped to the three value panels. Full rake green (710 runs, 0 failures), rake compat clean. Screenshot-verification caught a stale main-repo preview server squatting port 8000 (served the OLD 3-grid artifact -- the same EADDRINUSE trap noted at M8-6/M8-17); confirmed on a fresh PORT=8011 that render.js renders the scoreboard correctly. Local ruby 4.0.x is NEWER than the 3.3 CI target -- CI is the 3.3 authority (not yet pushed). Gate-9-runbook.md written (preview eyeball, golden bless, merge+deploy EXPECT PUB LIVE 26/26 key count unchanged, live verification with a daily-04:45-append soak note, the D9-a..g decision list mapping each item to its scoreboard row + --json field; D9-a and D9-d are NOT on the card and their evidence locations are called out honestly). Shadow fields NOT verdict-gating -- any flip is a later reviewed packet.

2026-08-11 · M9-5/6/7/8/9 (wave 2) · bd8a898..d085814 · Phase 9 wave 2, the LPPL STATISTICAL packets -- Opus impl, Fable spec+review, shadow-first (every new number ADDITIVE + report-only; no existing score/filter/verdict/frozen-aggregation touched, Golden Rule 4 verified end-to-end: post-wave trend/fit/logperiodic frozen fields byte-identical to the pre-wave recording). M9-5 (bd8a898): fit --json += b_negative (fitted B<0 sign restriction) + damping=m|B|/(omega|C|) + damping_ref_threshold 1.0, wired into NOTHING; pure Lppl.fit_report_flags, exact-value unit tests either side of each condition. Real cache: b_negative=true, damping=0.4132 (<1.0, condition NOT met). M9-6 (ccae0c7): symmetric-null shadow -- new Lppl.power_decay_fit_v2 (same coarse+refined pass the LPPLS fit gets + RMSE-selected tc so tc-dependent row counts can't tilt it late; original power_decay_fit byte-identical, characterization-pinned so logperiodic is unaffected); fit --json += null_v2{tc,rmse,at_grid_edge} + improvement_v2. ARTIFACT CLEARED on real cache: original null tc pinned at the +10d grid edge (rmse .0804) -> v2 tc -22d, at_grid_edge=false (rmse .0789), so the honest RMSE improvement is 27.9% vs the frozen 29.2%. M9-7 (e2dc9dd): AR(1)+GARCH(1,1) parametric bootstrap as a stdlib shadow second null -- pure-Ruby Nelder-Mead conditional MLE (fallback + fitted:false on non-convergence), 1000 sims burn-in 500 seeded, EACH sim a synthetic price window re-fit through power_decay_fit then Lomb-Scargled (symmetric with the observed side; the frozen AR(1) bootstrap skips this refit); logperiodic --json += p_value_v2/sims_v2/garch{ar1,omega,alpha,beta,fitted}/runtime_v2_s, AR(1) path untouched. Real cache: AR(1) p=0.376 -> GARCH+refit p=0.238 (both non-significant, garch fitted=true alpha .202 beta .600), runtime 18.22s -- under the 120s budget so DEFAULT stays 1000 (--sims-v2 N / LPPL_SIMS_V2 override; the env knob shrinks it for the test suite). M9-8 (d2a03da): long horizons 365/730 report-only in a SEPARATE trend_scores_long.csv + additive per_horizon_long (parallel map, report_only:true; the frozen per_horizon 30/90/180 stays byte-identical, excluded from the +-1 band + score). Real cache trailing-1y (53 evals): the pl_full-vs-best-rival differential climbs monotonically with horizon and FLIPS POSITIVE at 730d -- 365d mean/eval -0.1093, 730d +0.1598 (vs 30d -0.61/90d -0.43/180d -0.22) -- our data agrees with the published claim that the power law wins at 24mo. Earliest eval 2017-01-01 (EVAL0-bound; h=730 fit-cutoff ~2015-01 +730d ~2017-01, arithmetic confirmed). Backfill 3013 rows ~1.1ms compute (O(1) RangeReg prefix sums), steady-state ~0.003ms/day -- negligible. M9-9 (d085814): PL+LP1 rival -- THE PHASE'S DEEPEST DELIVERABLE. Stage 1 lp1_check.rb (standalone research script, NOT in publish): full-history power law in ln(age), Lomb-Scargle peak omega 8.70, single rigid-mode explained variance R^2 34.3% -- reproduces SBI's 8.75 / ~35%. Stage 2 trend rival pl_lp1 = [1, ln(age), cos(w ln age), sin(w ln age)] with omega rigid at the stage-1 peak (refit-free; prefix-sum Lppl::PlLp1Reg, O(1)/fit), fitted like pl_full on [0,j], Gaussian log-score; scores go ONLY to trend_scores_lp1.csv + an additive pl_lp1{per_horizon,omega,clock} section, NEVER into the best-rival max or bf. Real cache (raw per-eval mean logscore, 53 paired evals): pl_full vs pl_lp1 -- 30d -0.8085/-0.8037, 90d -0.8208/-0.8398, 180d -0.8336/-0.8700: the single mode explains 34% of IN-SAMPLE residual variance yet adds essentially nothing out-of-sample and slightly HURTS at longer horizons -- strong evidence the log-periodic oscillation is a descriptive in-sample artifact, not a predictive signal. The ln(age) omega is labelled NOT comparable to the post-peak ln(tau) omega everywhere (SBI's caution). Every commit: full rake green (676 -> 706 runs, 0 failures), rake compat clean, contract impact additive-only (contract tests + shape pins in the same commit). Local ruby 4.0.6 is NEWER than the 3.3 CI target -- CI is the 3.3 authority (not yet pushed). No chart/web changes (shadow fields reach the dashboard at Gate 9, not now). Decision items D9-b..f now have their data live in --json for the owner's rulings.

2026-08-11 · M9-1/2/3/4/10 (wave 1) · aa2d1a7..54db48e · Phase 9 wave 1, LPPL statistics honesty -- Opus impl, Fable spec+review, shadow-first (every new number additive or a wording change; no score/weight/threshold/verdict touched, Golden Rule 4). M9-1 (aa2d1a7): renamed the trend "Bayes factor" to "cumulative log predictive-score differential (log10)" in trend.rb/METHODOLOGY/LPPL-SUITE (one "(formerly 'Bayes factor')" per surface; deprecated bf/bf_by_horizon --json keys kept); additive per_horizon:{h:{sum,mean_per_eval,n_evals}} -- mean_per_eval is the density-invariant reading feeding D9-b. SBI-3.1 cache-density reproduction (controlled, both --as-of 2026-08-11 read-only on the real cache): FULL daily 364 eval pts differential -459.14 [30d -222.50/90d -158.33/180d -78.31] mean/eval -1.262; THINNED every-7th 52 pts -66.65 [30d -33.10/90d -22.42/180d -11.13] mean/eval -1.282 -- the sum falls 6.89x for a 7.0x point drop while the per-eval mean holds (~2%), confirming the headline sum is a density artifact. M9-2 (fbe0a0b): empirical percentile was already the lead number, so this makes the Gaussian's reference-only status explicit (percentile.rb header, "Gaussian ref" headline label, METHODOLOGY Test 5); weight stays 0; no new field; chart has no percentile title so no golden re-bless. M9-3 (2ff4920): "evidence index (not a probability)" framing for the scenario AND lppl composites (lppl.rb header, METHODOLOGY x2, README, web/guide.html); lppl.rb --json gains additive omega_xcheck{fit_omega,ls_omega,delta} (the two independent omega estimates SBI flagged as never cross-checked -- real cache 8.7/8.5/0.2) and trough_stability lifted to a first-class top-level field (null until --history accumulates); contract tests (aggregator + as-of + stale variants) updated same commit; D9-a already filed. M9-4 (2dc4fa1): "descriptive cycle heuristic" wording (envelope.rb/METHODOLOGY/LPPL-SUITE/guide.html) + additive report-only freeze_candidate -- the 2022 strong bound measured against a trend fitted only through the 2022 trough (real 0.358 vs the live drifting bound 0.439, quantifying the drift D9-g's freeze rule would remove). M9-10 (54db48e): additive delta_ln_age on trend --json -- ln(age_end)-ln(age_start) of the trailing-1y window, 4dp (real 0.0584 at age ~17.6y; the calendar year is a sliver of the model's ln-age clock). Every commit: full rake green (676 runs, 0 failures), rake compat clean; contract impact additive-only. guide.html headless-screenshotted and read -- evidence-index / descriptive-cycle-heuristic terms integrate cleanly, no layout breakage. Local ruby is 4.0.6, NEWER than the 3.3 CI target -- CI is the 3.3 authority (not yet pushed). Chart specs / goldens deliberately untouched (chart wording deferred by packet scope); M9-5..9 are a later wave.

2026-08-11 · Phase 9 opened · -- · Gate 8 closed 2026-08-10 (PR #9 merged, hotfix #10 for the sourced-env gate blocker, deployed, live-verified: 26 keys, new cards/tabs/badges, CSP+Vary live, guide 200, agents publishing on the new code). Phase 9 = LPPL statistics revision per the SBI review: packets M9-1..10 elaborated, decision items D9-a..g filed, shadow-first discipline. Branch phase-9 in ~/Dev/mimir-phase9; main tree stays on main for the agents.

2026-08-10 · M8-18 · ca31fa4..1c6b755 · GEX renames + MSTR trend + five display rulings (owner-ruled 2026-08-10; Opus impl, Fable spec + review, mimir-design bound). SIX rulings in a few commits. R1: chart keys renamed chart:gex_profile -> chart:gex_btc, chart:gex_trend -> chart:gex_btc_trend (producer keys and builder fns unchanged) across CHARTS registry, goldens (git mv), CARD_ORDER, index.html PRODUCERS map, deploy.rb CHART_KEYS sentinel, every test pin, runbook. R2: new chart:gex_mstr_trend -- daily MSTR GEX trend (fourth GEX tab, card is now [BTC][MSTR][BTC TREND][MSTR TREND]); scripts/gex_trend.rb --json gains an additive top-level 'mstr' {series, stats} built by a new BTC::GexHistory.mstr_series over the MSTR entries of each snapshot's `us` capture (walls under 'strike'), on MSTR's own dollar axis (raw $, not $k), no max-pain cross-check; contract test + unit tests + synthetic fixture 'mstr' block. R3: the (p) VENUE (c) toggle widget moved from a right-hand column to a FIXED 2x3 grid at the TOP-RIGHT of the BTC GEX plot (IBIT FBTC BITB / DERI ARKB GBTC, absent venues collapse), an owner-ruled EXCEPTION to "side panels go right". R4: both GEX profile tabs cut left/right margins (right 92->12, left 52->42) for a visibly wider plot; gex_btc top 56->66 so the raised wall labels clear the 2-row widget. R5: scenario_strip title gains a composite-drift arrow (latest minus 7 readings back; >+0.02 ↗, <-0.02 ↘, else →; no arrow under 2 readings). R6: card badges are dot-only now -- age/ttl/time moved to an instant hover/focus bubble (the header-ldot pattern; badge keyboard-focusable, bubble right-anchored + flip-up, built from data attrs on open); render.js makeBadge/setBadge/refreshBadge, index.html ticker simplified (.age span + fmtAge/paintBadge/initBadge gone), both pages dot-only, index.html CSP hash recomputed. R7: LPPL three-panel grids denser (heights 19%->23%, gaps ~14/11% -> ~4/4%). Key count 25 -> 26 (one new chart): every count/status pin. Goldens (gex_btc, gex_btc_trend, gex_mstr_trend, gex_mstr, scenario_strip, lppl_regime) PROVISIONAL. Verified: rake green (675 runs, 0 failures; web:test 27/27); PUBLISH_DRY_RUN=1 -> 26/26 keys, 0 skipped; headless 1440+2000 screenshots read against every ruling; Playwright on index.html -- badge bubble on hover AND keyboard focus ("age 2m16s · ttl 1800s · 19:31Z"), venue (p) toggle flips off, all four GEX tabs render a live canvas (MSTR TREND = real 34-day MSTR history), vol surface MSTR tab still swaps, console clean, 3x2 grid intact, 26/26 fresh. NOT live until the phase-8 gate merges + deploys.
2026-08-10 · M8-17 · d0a3590+07ca219 · MSTR vol surface tab (owner-ruled 2026-08-10; Opus impl, Fable spec + review, mimir-design bound). The vol card's SURFACE half gets a [BTC][MSTR] tab pair -- an MSTR vol surface with the same set-up as the BTC one, tabbed onto the same stacked card (surface section on top, basis below). Producer scripts/vol_mstr.rb mirrors vol.rb's shape and --json contract ({ts, mstr_spot, tenors[]} with the identical tenor field set) but reads MSTR via the CBOE delayed-quote chain (book mapping copied from vol_spread.rb's MSTR leg -- iv already a fraction, u = current_price; not a refactor), tenors DEFAULT_TARGETS 7/30/90, aborts nonzero when CBOE is down (vol.rb precedent); contract test pins the frozen field set + the abort against the recorded MSTR fixture; same CBOE endpoint so no new health source. Chart: vol_surface + vol_surface_mstr share the SAME option body via a new private vol_surface_option(vol, title_prefix) -- the two public builders differ only in the title, so vol_surface's existing golden is byte-identical -- and both carry tab_group 'vol'/group_style 'stack'/tab_pos 0, so buildStackedCard collapses them into ONE tabbed SURFACE section (mini [BTC][MSTR] tab bar; a click swaps the visible chart div + key/bubble/badge and resizes the revealed chart) while vol_basis (tab_pos 2) stays a plain section below; default active = lowest CARD_ORDER rank (BTC). CARD_ORDER + index.html PRODUCERS 404 map + its recomputed CSP sha256 in web/_headers updated; index.html re-inits the ticker on stacked cards per member (the stack rebuilds on each add). Key count 23 -> 25 (a producer AND a chart are both new keys -- the task brief's "24" undercounted): pipeline comment, publish-pipeline status/count/skip pins (25/25 status, 25 real PUTs, 24 index members), Gate-8 runbook (25 keys / 25/25 / KV-quota ~300 writes/day), index banner. Fixture payload_vol_mstr.json is the real producer output generated offline through the contract harness (recorded CBOE MSTR fixture, vol clock); golden chart_vol_surface_mstr.json blessed PROVISIONAL (owner re-blesses at Gate 8, no existing golden drifted). Verified: rake green (665 runs, 0 failures; web:test 26/26); PUBLISH_DRY_RUN=1 -> 25/25 keys, 0 skipped (live read-only GETs); Playwright on index.html -- [BTC][MSTR] surface tabs (BTC default, real curves), clicking MSTR swaps to chart:vol_surface_mstr ("MSTR vol surface · ATM 30d 70.4%"), basis section untouched, all 8 dashboard badges tick, console clean, 3x2 grid intact, 25/25 fresh. NOT live until the phase-8 gate merges + deploys.

2026-08-10 · M8-16 · 79937fc+7bc21d0 · vol_spread daily trend, stacked below the bars (owner-ruled 2026-08-10; Opus impl, Fable spec + review, mimir-design bound). chart:vol_spread becomes a stacked card like vol_surface/vol_basis: current per-tenor spread bars on top (unchanged), a NEW daily-trend chart below plotting the 7/14/21/45/90d spreads over time. Producer: scripts/vol_spread.rb appends ONE row/UTC-day to data/vol_spread/history.jsonl (date-guarded like gex_snapshot.rb; written whenever >=1 leg is live, null legs kept as honest gaps), and --json gains an additive "history" field (trailing 120 rows trimmed to date + tenors[{tenor_d, spread_atm}]) -- contract test updated same commit (TOP_KEYS + shape + a date-guard test: two runs same fake day -> one row). Chart: new builder vol_spread_trend reads that history (SAME payload fixture as the bars, group_style 'stack', tab_group 'volspread', tab_pos 1, height 235; vol_spread gains the matching stack meta at tab_pos 0), one line per tenor scaled to vol pts (spread_atm*100, 1dp), null -> gap, filled dots size 6 per the sparse-data ruling, empty history still a valid option. Fixture gained a synthetic 10-day history (one null 45d day to exercise the gap); golden chart_vol_spread_trend.json blessed PROVISIONAL (owner re-blesses at Gate 8, no existing golden drifted). CARD_ORDER + index.html PRODUCERS map + its CSP sha256 in web/_headers updated; key count 22 -> 23 across every pin (pipeline comment, publish-pipeline status/count tests, Gate-8 runbook, index banner). Verified: rake green (660 runs, 0 failures; web:test 24/24); PUBLISH_DRY_RUN=1 -> 23/23 keys, 0 skipped (live read-only GETs); headless screenshot shows the stacked spread card (bars on top, trend below) with one dot per series from the single live day, 3x2 grid intact, 23/23 fresh; the 10-day fixture render (isolated repro) shows five clean trend lines with the 45d gap. NOT live until the phase-8 gate merges + deploys.

2026-08-10 · M8-13 · 6bca072 · Dashboard auto-refresh (owner-ruled BUILD 2026-08-10; Opus impl, depends on M8-12's files): an open tab now converges to the latest publish with no reload -- the gap that twice showed us stale data. render.js gains nextDelay(ttl_hint_s) (per-key re-fetch cadence = the payload's own ttl, hard 60s floor so nothing storms the Worker; missing/NaN/short -> 60s) plus releaseCard/forgetGroup to dispose a card's echarts instances and reset tab-group state for an in-place rebuild. index.html runs one poll unit per solo card, per tab-group, and for the universe table, plus a header poller: generated_at is the change signal (unchanged payload rebuilds nothing), a changed payload swaps that card in place (groups rebuilt as a unit, the btco table re-dropped into its quadrant) and the header repaints its dot cluster + pub/fresh tick; a failed fetch keeps the last good card silently; polling pauses on visibilitychange (tab hidden) and catches up stale units on return. Display behavior only -- no ttl_hint_s VALUE, chart spec, publish path, or golden touched. Verified in headless Chrome under the real CSP with an advancing-publish harness: card badges + the pub tick advanced to a newer generation with no reload and a clean console; nextDelay clamp unit-tested (DOM timer exercised in preview per the packet note). Gate green, web:test 24/24.

2026-08-10 · M8-12 · d8cc65d · Web hardening from SBI review finding C4 (Opus impl, mimir-design bound). Two defence-in-depth mitigations for the public-read dashboard: (a) worker.mjs's cacheable 200 now also sends Vary: Authorization, so once AUTH_TOKEN is enabled a shared cache can never hand a token-authorized response to an anonymous client; (b) a Content-Security-Policy for the static-asset dashboard lives in web/_headers (the HTML is served by wrangler [assets], not the worker) -- script-src carries NO 'unsafe-inline', only self + the ONE pinned ECharts CDN origin + the sha256 of each page's inline boot script, so an injected external script or inline handler cannot run; style-src keeps 'unsafe-inline' (pages have a <style> block + runtime element .style). render.js's three innerHTML sites that interpolated KV strings (errCard key, both static staleness badges' ttl_hint_s) now build DOM via createElement/textContent -- byte-identical output, a poisoned payload can no longer inject markup; constant-only innerHTML stays. Verified in headless Chrome under the real CSP header: no violations, the inline boot script executes, styles apply. New test/web/csp.test.mjs pins that the CSP is derived from the real CDN tag, has no script 'unsafe-inline', and stays in sync with each inline script's sha256 (a boot-script edit that forgets to refresh the hash fails the gate). Gate green, web:test 24/24.

2026-08-10 · OWNER RULING: BTCo frozen · -- · Owner stopped all BTCo (treasury) development pending a serious rethink, during the Gate 7 attempt: validate.rb showed XXI/DJT past the 120d staleness limit and MSTR shares a month behind after the 4-week pause, and the refresh attempt ran ingest in heuristic mode (ANTHROPIC_API_KEY missing from the env file), dismissing the BTC-count 8-Ks. Plans updated: ruling block + frozen items (M7-15b, D8-h, D8-e) in BACKLOG; Gate-7-runbook re-scoped (no BTCo validation; v1 = soaked suite with BTCo present-but-frozen; new decide items: keep/hide the card, keep/stop the btco-alert agent); DEV-PROPOSALS family D frozen; README BTCo section marked frozen with a do-not-trust note; CLAUDE.md phase line updated. Code/ledgers/tests stay in the tree, nothing deleted; any production retirement (card, agent, keys) goes through the decommissioning inventory at Gate 7. Focus now: Gate 7 (reduced) -> Gate 8 (vol/GEX) -> Phase 9 (LPPL statistics).

2026-08-09 · M7-17 · 1a33255+21d6dc3 · SBI consolidated review (compiled 2026-07-31, against phase-6 5227c75) triaged; the two phase-7-urgent findings fixed. C1: ingest_text.rb KEYRE used /x free-spacing, silently deleting the literal spaces in 6 of 10 keyword phrases -- "preferred stock", "issued and outstanding", "shares of common", "liquidation preference", "notes due", "aggregate principal" could NEVER match, so filing excerpts sent to the extraction model missed those sections (old tests pinned only single-word keywords). Fixed with \s+ gaps + phrase-level tests + contract-pin update in-commit. Outcome check on the real MSTR 8-K fixture: the old regex's only "preferred" hits were camelCase XBRL cover-page tokens (PreferredStock0001ParValue...); the fixed regex catches all 6 prose "preferred stock" occurrences. Blast radius (offline capstruct ledger scan): every ai-mode apply touching at-risk fields (shares/pref_liq/debt/converts) is dated 07-07..07-09 and was superseded by the 07-10/11 externally-reconciled M7-16 baseline sweep or an owner correction; residual exposure is false NEGATIVES only (convert/preferred sections the model never saw -> never proposed), covered going forward by the fix and backward by external reconciliation. C7: CI now runs rake health + fixtures:verify (was compat/test/web:test only -- registry drift + fixture digests were local-only gates). C2 (shares_diluted convention undefined) filed as decision item D8-h -- no semantics change without a ruling. Full LPPL-suite integration plan (R1-R12 -> Track A now / Track B pre-Gate-8 / Phase 9 stats revision) in local notes .docs/lppl-improvements.md.

2026-07-13 · M8-8/9/10 · 306b36a..c62527f (cherry-picked) · Data-integrity mechanics complete (owner-ruled same day after the blind-zero incident; Opus impl, Fable review + visual verify): (8) markers at write time -- scenario history line +blind:true (ZERO live modules, read from the F-12 unavailable flags) / +unavailable:[names] (partial), lppl ledger +stale_input:true (price cache older than run-day D-1, 24/7 no weekend slack), snapshot date-guard now re-captures an errors-carrying today-file (was: a partial file blocked its own retry forever); (9) ops/repair.rb same-day repair loop -- pure decision table (today-UTC only; blind/stale/errored-or-missing -> re-run producer + atomic tail-line rewrite or re-capture; REPAIRED old->new log lines; silent when clean), wired into run_publish.sh BEFORE publish (12 chances/day, no reinstall -- plist execs the repo wrapper), fail-soft so repair can never block a publish; (10) scenario_strip renders blind days as hollow points (verified in preview -- seeded 8-row history, the blind day reads as an open ring among filled dots; golden PROVISIONAL), publish status +BLIND:<suite> marker on the OLD pattern (publish_health surfaces it + forces !), Gate-8 runbook EXPECT + README ops paragraph. All contracts additive w/ same-commit tests; composite/scoring untouched. Gate 648 runs green. Remaining owner bits: golden bless at Gate 8; one-time blind:true stamp on the real 2026-07-13 row (predates the mechanics).

2026-07-13 · incident: overnight network outage · -- · gold offline ~00:10-06:15Z (second night running; 2026-07-12 had a single-tick DNS abort at 08:03Z): publish ticks 00:10/02:10/04:11/06:11Z all ABORTed on Socket::ResolutionError (whole run lost per tick -- abort-on-first-PUT); keep-last-good served 22:09Z data ~10h; 08:12Z tick recovered clean, live index stamp verified. Casualty: the 04:45Z suite-history append ran inside the outage window -> scenario history row 2026-07-13 is a BLIND ZERO (composite 0.0 NEUTRAL, all 7 modules fail-soft 0, and the compressed history line carries NO unavailable marker -- indistinguishable from a real neutral day). gex snapshot (06:15Z) + lppl append landed AFTER recovery, healthy. Decision item D8-g filed. NOTE for the Gate 7 eyeball today: the scenario strip's newest point is the blind row.

2026-07-12 · M8-7 · 6dbbe77+0286ed6 (cherry-picked) · Dashboard guide + title bubble (owner-requested same day; Opus impl, Fable plan + prose/visual review): web/guide.html single-file reader doc (header semantics, per-card What/How/Title-number/Cadence/Fails sections, GitHub deep links -- repo public), title-h1 instant bubble w/ orientation paragraph + guide/METHODOLOGY links, preview parity, Gate-8 runbook EXPECT. Prose accuracy rule enforced: every claim traced to METHODOLOGY/README/chart meta/code (btco verdict buckets verified against the lambda: DEEP-DISC<0.90 UNDER<1.10 FAIR<1.45 RICH<1.90 OVER); kept the honest blind-0 caveat (a dead source's fail-soft 0 reads like a healthy 0 on the strip). Agent screenshot review clean incl. 700px no-clip bubble proof (CDP raw-hover driver, Playwright unavailable offline). Gate 619 runs green. Same day: transient DNS outage aborted the 08:03Z publish tick on the first PUT (Socket::ResolutionError x4) -- keep-last-good held, network recovered, next tick heals; launchctl kickstart is permission-blocked for the loop (owner command supplied instead).

2026-07-11 · M8-6 · 26ffe17..6b2e139 (cherry-picked) · Family A stage 2 COMPLETE (Opus impl, Fable design direction + visual verify): pipeline PRODUCERS += vol:latest/vol:spread/basis:latest/gex:trend/gex:check, key count 13 -> 22 with every pin updated in-commit; four chart specs + payload fixtures + PROVISIONAL goldens (vol_surface/vol_spread/vol_basis as the new Volatility card, tab_group vol, SURFACE default; gex_trend joins the GEX card as [TREND] tab_pos 3 with the gex_check max-pain delta as title suffix, degrades gracefully); web/ = 404-copy map + 22/22 banner only (tab machinery generic). Agent self-caught a legend-swatch color bug; Fable verify first screenshotted a STALE preview server (EADDRINUSE silently left the old repo's server answering :8000 -- lesson: check the serve log before trusting a screenshot), then confirmed on the real serve: 5 cards, blessed quadrant order, [BTC][MSTR][TREND] + [SURFACE][SPREAD][BASIS] with correct defaults, compact titles, no theme leaks. Gate 619 runs green. README: Phase-8A tools section + ten-producer publish + stale D8-f line removed. NOT live until the phase-8 gate merges + deploys; production stays 13/13 phase-7.

2026-07-10 · M8-3 + M8-4 + M8-5 stage 1 + Coinglass seam · d2cb256..HEAD · Producer layer of family A COMPLETE, owner offline ("keep going" ruling): BTC::Coinglass seam (option/info, max-pain -- 400s without exchange param, OI-weighted funding; 3 soft SOURCES entries + ALLOWED_ENV) with fixtures recorded via sanctioned keyed probes (provenance in fixtures README) + a trimmed live CBOE MSTR chain. M8-3 gex_check.rb (Opus): our walls/flip vs Coinglass Deribit max pain, report-only per the ruling; live: nearest max-pain 63500 within 0.78% of our call wall, Deribit OI share 82%. M8-4 basis.rb (Opus): 12-tenor annualized basis + perp premium + OI-weighted funding means; agent flagged a funding-magnitude anomaly -> live cross-check vs Binance proved coinglass returns PERCENT (ratio 100.05), x100 dropped before the contract froze; live: smooth contango 2.5->4.1% ann, funding 0.010%/8h canonical. M8-5 vol_spread.rb (Sonnet, on the M8-1 seam): MSTR-vs-BTC ATM IV per tenor -- first live print of the treasury-leverage premium: +47..+53 vol pts (MSTR 77.6-87.8% vs BTC 30.7-37.2%). Gate 607 runs green. Stage 2 (Volatility card + GEX-card enrichment + publish wiring) next; KV-quota arithmetic: 13 keys now, +4 chart keys -> ~204 writes/day, ~20% of free tier -- Gate 5 carry-over check stays owner-scheduled.

2026-07-10 · M8-1 + M8-2 stage 1 · 2047bf2..a68e655 · Phase 8A opened (branch phase-8 off phase-7 e48ce84; owner-approved same day to run in parallel with the soak; main working tree stays on phase-7 so the launchd agents never execute unreviewed phase-8 code -- packet work happens in worktrees). First two packets, Opus implementation / Fable spec + review: M8-1 vol surface (additive Options.norm_cdf/bs_delta; BTC::Vol.surface -- ATM IV, 25d RR, 25d fly per 7/30/90d nearest-expiry tenor, nearest-strike no-interpolation v1, fail-soft thin tenors; scripts/vol.rb producer with frozen --json {ts,btc_spot,tenors[]}; gex_snapshot.rb writes data/vol_history/ alongside, fully contained so a vol failure can never touch the GEX snapshot). M8-2 GEX history analytics (BTC::GexHistory.series/stats over the daily snapshots nothing read; scripts/gex_trend.rb --json {ts,days,series,stats}, descriptive only -- no thresholds/scores per Golden Rule 4). Combined gate 562 runs green; outcome checks: gex_trend over the real 5-day history (long_gamma 5d run, flip_dist +2.00..+3.67%), vol.rb live against Deribit (739 instruments: ATM 30.8/33.3/37.2% upward term structure, RR25 -4.4/-6.3/-6.0 downside skew -- sane surface). Chart specs/cards = stage 2 (mimir-design pass). Note: Deribit book summary carries no delta field, so 25d points come from BS delta computed off mark_iv (definitions unchanged).

2026-07-10 · M7-16 sweep complete + M7-4 · 67e2078+ · Universe 8/8 baselined and externally reconciled: owner ran --baseline across every ticker; validate.rb post-sweep shows every US name agreeing with external refs (MSTR MATCHES the tracker mNAV; NAKA fixed to 4,467 @2026-06-11 with a ~40x share-count correction to 17.4M post-restructure; ABTC post-split 72.8M shares; BLSH 24,400 + debt 507.1M; DJT real straight debt 5.3M). XXI debt_face -> 0 (owner-approved; the pilot-1 double-count; prompt fixed + pinned in 77ed3c8). Residuals: 3350 rowless pending D8-f; MSTR ATM share drift (quarterly cadence); ABTC refs mutually disagree. M7-4 done: README rewritten (two-regime btco section, 4-agent ops, refreshed roadmap) + Gate 7 owner runbook in BACKLOG (validate green -> 6b eyeball -> soak grep -> decisions -> merge+tag v1 -> Phase 8 scope).

2026-07-10 · M7-16 · -- · Baseline-reset + per-field freshness gate (owner ruling verbatim in BACKLOG: ground truth AS OF NOW, then every ingestion tested against LATEST KNOWN GOOD). (a) Freshness gate: universe entries gain an additive as_of map; drop_stale_fields generalizes the btc guard to EVERY field at propose time ("nothing newer than the model -- no proposal"); apply blocks stale fields per-field with a printed note and STAMPS as_of dates on everything it lands (converts stamped via the dedup guard's survivors). (b) ingest --baseline TICKER: AI + server-side web_search takes the full dossier (entry, ledger tail, live refs from both aggregators + SEC dei) and returns a complete snapshot with per-field value/as-of/source (BASELINE_NOTE schema, contract-pinned incl. cash + corporate_actions -- the DJT/ABTC blind spots); ONE pending proposal; --review shows per-field provenance; apply REPLACES the entry wholesale (identity keys preserved) and ledgers form BASELINE. Prior-session failures this answers: XXI 4x-duplicated convert (dedup guard, committed separately 9a3db06), XXI btc regression below aggregate reality, ABTC invisible reverse split, MSTR ATM share drift. Offline flow tests end-to-end via a canned Anthropic response fixture (SYNTHETIC, registered); 6 new flow tests + message re-pins; M7-10 superseded. Pilot: owner runs --baseline XXI then ABTC.

2026-07-09 · M7-15 core + data session · 14efd6c+ · Owner rulings landed: MSTR 2029 convert face 3.0B -> 1.5B committed (held a day after a rejected commit; owner re-approved); 3350 tracker proposal + NAKA 10-K applied by owner and committed. Built validate.rb + validate_core.rb (13 tests): per-ticker objective reconciliation of the SERVED payload (KV v1:btco:latest) against StrategyTracker's published mNAV decomposed into the four input factors, btc counts from two aggregators, SEC dei cover shares -- answering the owner's "ratios not grounded in reality / wtf does XXI need" with named causes + exact next commands. First live run findings: ASST MATCHES externals exactly; MSTR -6.4% fully input-explained (share-count lag: Apr cover 350.4M vs tracker 371.6M -- ATM growth outruns covers); NAKA + ABTC btc counts off vs every ref (newer filings exist); BLSH shares available via SEC dei 20-F (150,833,916 @2025-12-31) despite the silent 6-K; XXI needs only a placeholder-clear ruling (refs concur); 3350 missing from the dashboard = dead stooq quote API (F-17) + no manual_px -> D8-f. DJT inputs all agree -- its 3.94 mNAV is definitional (no cash/non-BTC assets in the model), the M7-15b question.

2026-07-08 · M7-11 + M7-12 + M7-14 · -- · Structured-source adoption round 1 (owner "proceed" on the research architecture): lib/btc/coingecko_ref.rb (second BTC aggregator ref; --review now prints BOTH ref lines via the shared ref_lines/verdict_vs refactor), lib/btc/sec_shares.rb (SEC XBRL dei cover-page share counts WITH as-of dates; shares ref line when a proposal touches shares_basic; multi-class filers nil by design -- M7-13 closes that), ingest --tracker <T> (StrategyTracker feed row -> normal reviewed proposal, btc/as-of/shares only, TDnet PDF as url; built for 3350). 3 fixtures recorded + fake-transport routes; 3 soft health entries (21 sources registered); 21 new tests (unit refs + tracker/review flow surfaces). Also fixed in passing: the flow harness leaked SourceCache writes into the REAL data/source_cache/ (BTC_DATA_DIR now sandboxed). M7-13 (deterministic filing-iXBRL parser: per-class covers + convert tranches, kills the DJT-bug error class) elaborated, awaiting owner go.

2026-07-08 · DJT correction + data-source research opened · dc0c8df · Post-session smoke caught the DJT 10-Q extraction wrong twice over: the $1.0B 0% convert entered as BOTH debt_face and a tranche, and conv_price 0.034722 (the filing HTML renders the XBRL scale=3 denominator as "per $1.0 of Notes" -- it is per $1,000; 28.8 sh/$1,000 = $34.722) -> 28.8B phantom ITM shares, CEBE 0. Owner approved ("1y"); deliberate edit + CORRECTION ledger entry citing the XBRL tag evidence. Visible row unchanged at current px (net NAV genuinely negative). Owner then ruled the ingest process "unreliable and prone to errors" -> deep-research workflow launched on replacement/hardening data sources. Early live probes: CoinGecko public_treasury endpoint (free JSON, no key) covers all 8 tickers with CURRENT counts (MSTR 843,775 exact match; exposes 3350 seed as badly stale -- Metaplanet 43,000 vs our 15,555) but NO as-of dates; SEC companyfacts dei EntityCommonStockSharesOutstanding gives exact cover-page counts + dates for single-class filers (DJT exact match; NAKA one filing behind) but drops dimensional facts, so multi-class covers (MSTR, ASST) are absent.

2026-07-08 · M7-3 sessions complete · 6f1b0b3 · Owner finished the per-company ingest: DJT NAKA ASST BLSH ABTC real (placeholder:false), five new capstruct ledgers committed; MSTR's as-of guard held 843,775 @2026-07-05 through five older 8-Ks analysed in the same batch (ledgered, no regression). 6/8 real; XXI + 3350 remain placeholder seeds and BLSH shares_basic is null (6-K stated no outstanding count) -- residuals + decision items recorded in BACKLOG M7-3. Smoke run clean: BLSH renders dim --, no crashes, aggregate nil-tolerant. CI green.

2026-07-07 · M7-3 session (MSTR + tooling) · 9354ceb..34980f0 · Owner's first live session, MSTR fully real end-to-end: 843,775 BTC @2026-07-05 verified on the live dashboard against three independent sources. Mid-session hardening driven by owner catches: three older 8-Ks applied after the newest regressed the btc pair -> restored from the post-apply commit (CORRECTION entry) + as-of guard at apply time (6bb720b) + stale-btc filter at PROPOSE time (9d46a65, owner: the review should never even propose it); 10-Q weighted-average shares mis-extraction -> SCHEMA_NOTE now demands cover-page OUTSTANDING counts or null (contract pin updated same commit), owner read the cover page -> 350,447,872 basic as a deliberate edit, diluted null; --dismiss-all [--ticker] bulk cleanup (d865d3a); --review prints paste-ready apply/dismiss command lines (d8fce93, owner: never make the reviewer assemble commands); newest-first discovery + universe recomposition to 8 companies with owner-ruled CIKs (143224c; SMLR+GME dropped, BSTR deferred to its SPAC closing); null-shares rows -> nil metrics, never Infinity (34980f0, FloatDomainError). "MSTR reported stale" resolved as an un-refetched tab (live payload proven clean) -> refetch bundle proposed, awaiting owner go.

2026-07-07 · M7-9 · 866eb97+df31a97 · Third-party sanity refs in --review (owner mid-session request): BTC::TreasuryRef scrapes the bitcointreasuries.net table via SourceCache ('treasury_ref', alias 3350->Metaplanet), prints "ref: N BTC (bitcointreasuries.net, as-of ...) -- proposal matches / ⚠ diverges N%" (>2%) per proposal; advisory only, silent when dead; soft health-registry entry. Coinglass REJECTED for cause: no per-company treasury endpoint on our tier (ETF flows only). Fixup df31a97: the fixture-inventory check scans COMMITTED files -- the new fixture passed locally while untracked, red in CI; lesson recorded (git add fixtures before the local gate).

2026-07-07 · M7-8 · 4dc617c · Per-source last-good cache + partial computation (owner priority: "one dead provider must never blank a card"): lib/btc/source_cache.rb read-through cache over BTC::Http (48h MAX_STALE_S hard cap, data/source_cache/, atomic writes, never caches errors, BTC_DATA_DIR seam); gex family + btco adopted (deribit spot via new cached Deribit.index sharing one 'deribit_index' entry; index_price left uncached -- scenario modules DELIBERATELY out, score-0 vs stale-reuse is a Phase 9 research decision). Payloads additive: 'sources' [{name,as_of,stale}] always present, stale flags only when true; 'DERI! C' series names + '· stale:' title suffixes ride the existing renderer machinery; renderer-side px_stale display deferred to a web packet.

2026-07-07 · INCIDENT: agent wiped live session data · -- · The M7-8 implementation agent, running in THIS tree while the owner's ingest session was live, deleted scripts/btco/capstruct/ and reverted universe.json (rm -rf + git checkout, ~5x each) treating the owner's fresh data as its own test debris. Orchestration fault: a code agent was given a tree hosting live owner data with no isolation. Contained via SendMessage; recovery cheap (the data was one commit old -- commit-after-every-apply paid for itself same day). Standing rules: code agents get worktree isolation when the tree hosts live session data, and every agent prompt now carries explicit no-destructive-git-command constraints.

2026-07-07 · M7-6 · 08c709c · Pipeline keep-last-good hardening (incident response, same morning): the 09:34Z tick during a Deribit outage dropped gex keys from the index (vanishing chart) and published a NaN gauge from a fail-soft payload. Fix: build_index carry: keeps last-good membership for skipped keys (previous index from KV/out_dir), junk-chart guard skips chart builds on 'unavailable' inputs. Both proven on the live surface during the outage window ("made it worse" -> fixed and shown fixed at the owner's surface).

2026-07-07 · M7-5 · f1134eb+2c3a348 · Daily evidence agent + content-recency guard (the frozen-evidence durable fix): ops/suite_history.rb wrapper+plist as the 4th managed agent (06:45 local) carrying the retired cron's --history duty (lppl --history owns the price-cache update; scenario --history); PUB! gains the OLD flag when any published tail's newest content entry exceeds STALE_EVIDENCE_H=30. Verification discipline written into CLAUDE.md + DEV-LOOP.md 6b same day (2c3a348, owner-ruled): outcome-first vs independent references, contradiction protocol, decommissioning inventory, content-progress invariants, full surface checklist. CI-red lesson folded in (69d3fca): local ruby is newer than the 3.3 target -- CI is the 3.3 authority, watch it after every push.

2026-07-07 · INCIDENT: frozen evidence trails · -- · Owner caught the dashboard serving Jul-6 data with fresh envelope stamps ("stop pretending everything's fine"). Root cause: the owner's old daily cron (suites with --history at ~04:54Z: price-cache update + lppl ledger/fit-history + scenario history appends) was retired at the Gate 5 install and NO ops agent took over the duty -- the publish agent deliberately runs lppl --skip-update/no --history, so bi-hourly publishes re-stamped frozen content. Loop compounded it by reporting health from envelope stamps + PUB token (both blind to content freshness) -- ttl-mismatch analysis was real but secondary. Remediated: manual --history runs 09:35Z (ledger + history + prices current), visible on-site from the 11:34Z tick. Same tick: transient Deribit 503 -> gex:combined + chart SKIPPED, keep-last-good served 07:34Z (fail-soft worked). Durable fix = M7-5 (daily evidence agent + content-recency guard), owner ruling pending. Soak note: 2026-07-07 · lppl:ledger+scenario:history · retired cron, ops gap · ~29h stale.

2026-07-06 · M7-2 · d343851 · Daily BTCo discovery alert (opus vs fable spec): additive ingest --dry --json contract, ops/btco_alert.rb (ING n! / quiet empty / ING ? -- always exit 0), third launchd agent (07:45) under ops:install/status/uninstall, ingest cat appended to the tmux right-align run. Fable review catch: idempotence check would have skipped already-installed bars (gold) -- in-place append-upgrade variant added + pinned. Integration proof: submissions endpoint only, no state.json.

2026-07-06 · M7-1 · 8285592+f2b6514+fd6e325 · Ingest flow characterized (opus, 12 tests, zero production diff) -- 80% of ingest.rb had never run. Three findings, all fixed by fable same evening: pending-dedup dash mismatch (silent proposal rewrite), backup-stamp same-second collision (batch kept one backup), and a Golden Rule 6 violation in the M1-11 contract test (copied lib/ reset the injected transport -- it silently hit the real SEC; symlink fix, verified under broken proxy). Standing lesson: sandbox harnesses SYMLINK lib/, never copy.

2026-07-06 · gate-6 CLOSED · 5227c75 · Owner merged PR #7 same day Phase 6 was elaborated -- five packets + two owner-feedback fix rounds (checklist rewrite + preview fixes), all shipped in one session. Ledger promoted by owner (rake lppl:promote: 275 entries, backups verified); 13 keys publishing live on the bi-hourly agent; tabs signed off on preview. Remaining owner action from the checklist: rake deploy (tabs + full LPPL curve to the live page). Soak review + novo promotion carry to the first gate after ~Jul 13. Branch phase-7 opened.

2026-07-06 · M6-6 · -- · Owner Gate-6 preview findings, both fixed (fable): (1) tab click pinned the info bubble over the chart -- the card-head :focus-within rule fired for the tab buttons; bubble focus-trigger scoped to .hover elements (key/ⓘ) in both pages, keyboard path intact (ⓘ keeps tabIndex). (2) markline labels collided with the chart title while panning + adjacent PW/flip garbled ("PWlip") -- both GEX charts get a two-band label zone (grid top 30->56; flip/spot lower band, CW/PW raised offset [0,-14]); banding pinned in exact-value tests; both goldens re-blessed after Playwright 7/7 + crops at default and zoomed-out geometry. Owner ran the promotion: live ledger 275 lines, fit_history 185, backups verified.

2026-07-06 · M6-5b · -- · Owner rejected the Gate 6 checklist ("MIGHT be machine-readable but definitely NOT human-readable") -- rewritten runbook-style (3 steps, one command each, EXPECT lines, background quarantined) and the promotion paste-block replaced by interactive rake lppl:promote (diff -> y/N -> timestamped backups -> merge BOTH history files; refuses CI/non-TTY like ops:*). Lesson reinforced: EVERY owner-facing procedure gets the runbook treatment + a task wrapper when it involves pasting commands; gate checklists are owner docs, not loop specs.

2026-07-06 · M6-5 · -- · README (replay/backfill/13-key/tabs capabilities, honest about promotion pending) + Gate 6 checklist in BACKLOG: ledger promotion (paste the two printed commands), MSTR/tabs visual sign-off, 13/13 + 156 writes/day expectation shift, soak review if >= Jul 13, merge. Phase 6 packets complete same day they were elaborated.

2026-07-06 · M6-4 · 4c96867+e95989a · GEX card tabs = 4th renderer hook tab_group/tab_label/tab_pos (opus vs fable spec, D7-c Option A): shared card, tab_pos order (BTC leads over alphabetical), reveal+resize, title/bubble/badge swap compatible with both pages' tickers, append guards; goldens PROVEN unchanged (meta lives in envelope not option -- implementer corrected the spec premise). Fable review fix: roving tabindex without arrow handlers made the inactive tab keyboard-unreachable -> all tabs naturally focusable. Playwright 6/6 both pages; SKILL.md hooks 3->4. Fixup e95989a: M6-3 fixture+golden were untracked (commit -am lesson) -- 3 intermediate commits red in CI.

2026-07-06 · M6-2 · cf494e2 · Staged backfill driver (fable): rake lppl:backfill / lppl:backfill_diff, BTC_DATA_DIR staging isolation, resumable, promotion commands printed never run. EXECUTED: 273 days 2025-10-06 (detect_peak == D7-a's Oct-2025 peak) -> 2026-07-05, ~14 min, stage-B overlap diff MATCH on every field both recorded days. Staged story: STRESSED throughout, bf -333 -> -426, ratio 1.11 -> 0.47, fit from ~day 90.

2026-07-06 · M6-1 · 1d248ec · lppl --as-of replay (opus vs fable spec): in-memory strict-< price truncation, all four wall-clock anchors frozen (additive now: kwarg on BTC::Report.report), trend cache read-filter + append suppression (density-sensitive BF), fit stability window filtered, --tmux refused, additive as_of json field. Opus hardening accepted: calendar-rollover abort. Smoke: replayed 2026-07-05 == recorded ledger entry on all six fields, read-only md5-verified, 2.9s/day -> full backfill ~15 min.

2026-07-06 · M6-3 · c8c117f · gex:mstr producer + sibling chart builder (opus): net-gamma-per-strike bars coloured by sign, spot/flip/CW/PW marklines, spot+-30% zoom, gex_profile framing for the future shared tab card. Implementer corrected the spec: 11 -> 13 keys (not 12; producer AND chart), key shape gex:mstr not gex_mstr:latest. All count literals repinned (tests, RUNBOOK, index.html banner); fixture + golden recorded; dry-run 13/13.

2026-07-06 · gate-5 CLOSED · 1d065b9 · Owner merged PR #6 (phase-5 -> main) after the gold staging install proved the full ops chain live (scheduled bi-hourly publish fired unattended: 11 written 0 skipped -> KV; snapshot archived with date-guard; tmux token on the owner's bar; render.js m5-hd3 deployed). Owner ruled proceed to Phase 6 same day -- the 48h window was folded into the merge decision; the week-long soak continues in parallel (daily 1-min check) and is reviewed at the first gate after ~Jul 13. Branch phase-6 opened; packets M6-1.. elaborated.

2026-07-06 · phase-swap · -- · Owner ruled phases 6 and 7 swapped: LPPL backfill + MSTR GEX panel first (autonomous), BTCo ingest pushed back (owner-interactive). v1 tag moves with the ingest to Gate 7 per the standing real-data ruling; soak review lands at whichever gate follows the week. Decision-item IDs (D6-a/D7-a/b/c) keep their historical labels.

2026-07-06 · M5-7 · 2f9149c · rake ops:tmux (owner ruling after the manual merge fumble): inspects the live server, idempotent when the token exists, proposes ONE fitted variant (free line vs own-align-run merge -- the live-verified form), reversible apply via tmux set -g + interval offer, ALWAYS prints the paste-to-persist lines, never edits ~/.tmux.conf; real path baked in everywhere. RUNBOOK section 3 = one command. Novo tmux step is now: rake ops:tmux, y, paste one line.

2026-07-06 · goldens-blessed · -- · Owner eyeballed preview.html and signed off ("preview looks fine now"): the 2026-07-06 payload-refresh goldens, the two-round gauge fix, the unified dot-only header, and the shared in-quadrant BTCo table are all owner-blessed. First fully-scheduled publish tick fired 15:29:22Z (StartInterval honoured, no kickstart).

2026-07-06 · unified-table · 65a6423 · Second unified-view gap (owner): the BTCo sortable table was index.html-only. buildBtcoTable + in-quadrant attach moved to render.js (rev m5-hd3); preview renders the identical card from btco_latest.json. Playwright pins table+290px shrink+3 sort states on both pages.

2026-07-06 · unified-header · d414fd1 · Owner ruled unified view ("everything on the same page design-wise as we sign off"): the dot-cluster/bubble/pub-slot builder moved into render.js (MimirRender.liveHeader, rev m5-hd2); preview.html dropped its two-row strip and delegates to the same builder as index.html (failure path -> directive banner). Playwright dot suite green on BOTH pages. Divergence now structurally impossible.

2026-07-06 · header-dots+gauge-2 · cf8cee8/fe2034d · Owner ruled the header: liveness collapses to a right-aligned dot-only cluster (key@HH:MM text in a shared hover/focus bubble anchored to the header's right edge -- never clips; dots are real buttons, aria-label = bubble text; Playwright pins rightmost/leftmost hover + keyboard). Gauge garble round 2: the REAL remaining collision was tick labels crowding each other at the 290px card height (the height hook) -- my 420px repro had lied; splitNumber 5 + 9px labels, verified at true geometry, golden re-blessed. Skill gains the test-at-real-geometry recipe; ruling in DASHBOARD-DESIGN.md.

2026-07-06 · gauge+fixtures · faaa3b3 · Owner spotted the live btco gauge rendering its series title over its own tick labels -- title hidden (info already in chart title + band detail; mimir-design labels-never-collide). Owner also requested a payload-fixture refresh (recorded 2026-07-05, four design rounds ago): re-recorded from a live dry-run, all four goldens regenerated + self-screenshot-reviewed, title pins re-pinned (NEUTRAL +0.08 / stress 69), lppl null-ledger test made fixture-size-independent (pinned counts, not emptiness -- it silently assumed the Gate-3 single-entry ledger). Real regime flip observed in the fresh data: etf_flows -1 -> +1 (the U1 flow-decay narrative arriving on schedule).

2026-07-06 · brrr-drop · 832b1cb · Owner ruled the BRRR decision item: dropped from gex_btc_combined's ETF sweep (CBOE 403s the rebranded Valkyrie ticker every run; venue was dust-sized, combined totals unaffected in practice). gex_us.rb's annotation list keeps the inert entry -- it only fetches argv tickers. Next scheduled publish stops logging the 403.

2026-07-06 · M5-2b · 137d13f/de7afde · Owner review of the live tmux line: colours useless in an already-bright bar -> plain token with '!' as the single attention flag (severity reads from the age; amber/red tiers dropped), and the RUNBOOK moves it to a SECOND tmux status line (status-format[1]). Contract updated same-commit, byte-exact pins. The live check of the new token immediately exposed a latent defect: rake test clobbered the real /tmp/publish.status (pipeline tests wrote the hardcoded path) -- Report.status/Pipeline.run gained injectable dir kwargs (production defaults unchanged), tests write into their tmpdir, verified byte-identical across a full gate. Also self-caught: touch -t reads LOCAL time -- restoring the status mtime needs ruby File.utime with an explicit UTC Time.

2026-07-06 · M5-6 · 95a48a8 · Owner ruling mid-install ("copy-paste mess... wrap in interactive script"): lib/btc/ops.rb + rake ops:install|status|uninstall -- pre-flight table, plist render+bootstrap (bootout-first reinstall), per-agent kickstart prompts with POLLED log verification (marker+summary, status-file freshness, dated snapshot; injected clock/sleeper), PASS/FAIL table; refuses CI and non-TTY (the latter locks the loop out structurally -- proven live). RUNBOOK 1/2/4/8 collapsed to the tasks, manual sequence quarantined as fallback. Gold staging day-0 ALSO closed live: kickstarted publish reached KV (publish LIVE: 11 written; index generated_at = kickstart minute), tmux line green, snapshot date-guard verified; three doc-literal bugs found by the live run (state = not running; zsh inline comments; skipped-is-a-pass) fixed same-day. Upstream finding: BRRR 403 from CBOE (fund rebranded) -- fail-softs per design, venue-list ruling queued as a decision item.

2026-07-06 · M5-4 · -- · Gate 5 checklist made concrete in the backlog (day-0 install steps -> 48h-green condition -> parallel soak closing at Gate 6) and README refreshed to the Phase 5 capability set: new Ops-layer section (prepared agents, snapshot archive, tmux line, offline health audit), not-implemented list rewritten to the approved P6-P10 roadmap (placeholder-universe caveat stays). Phase 5 packets complete; Gate 5 is the owner's.

2026-07-06 · M5-3 · 03ae48e · docs/RUNBOOK.md: nine numbered owner procedures with EXPECT lines (agent install/verify, tmux line, pause/uninstall, token rotation, namespace re-creation, key purge, stale-everything recovery in strict diagnose order, weekly soak checks) + quarantined Background; secret-safe (presence-only checks, editor-only edits). Opus draft; fable review corrected stdout literals (publish LIVE: 11 written vs the status-file LIVE 11/11), added --binding MIMIR to kv delete, realistic post-kickstart waits.

2026-07-06 · M5-5 · 78f24d1 · ops/gex_snapshot.rb: daily dated archive of gex_btc_combined + gex_us IBIT MSTR --json (verbatim, local-only under data/gex_history) -- date-guard idempotency, per-capture redacted errors, atomic tmp+rename, exit 1 only on both-fail; wrapper + StartCalendarInterval 08:15 plist in the M5-1 shape. Sonnet vs spec, no deviations. U3 (Phase 9) consumes this archive.

2026-07-06 · M5-2 · 732b2d1 · ops/publish_health.rb: tmux status-right one-liner over the frozen publish.status format + mtime -- green fresh / yellow amber-or-incomplete-or-DRY / red stale / PUB ? fail-soft (always exit 0). New frozen --tmux contract, 12 byte-exact pins (injected clock + utime, no sleeps). Sonnet vs spec, no deviations; review: ops/ joined rake compat RUBY_DIRS.

2026-07-06 · M5-1 · 63c6157 · ops/run_publish.sh (env-file sourcing w/ secret-free exit-78 refusal, ~/Library/Logs/mimir logging, PUBLISH_DRY_RUN=0, exec passthrough so launchd alarms on publish failure) + com.mimir.publish.plist template (__REPO__ placeholder, StartInterval 7200 per D5-a, RunAtLoad false). rake health gains the offline ops scan: bash -n, dry-run guarantee, repo-wide --apply ban (shells AND plists -- review addition), rexml plist checks (plutil absent on ubuntu CI). Opus implementation vs spec; wrapper behavior pinned by shelling tests under a fake HOME.

2026-07-06 · plan-v1.x · -- · Multi-phase plan approved (owner): P5 ops+preservation (M5-5 gex snapshots added; ingest agent REMOVED from P5), P6 ingest-to-real-data (Gate 6 = soak close + v1 tag), P7 LPPL backfill + MSTR GEX panel, P8 Coinglass groundwork/upgrades (improvements.md), P9 scenario v2 U-modules (scenario_upgrades.md), P10 dashboard round 2 (+D4-a). Rulings: D5-a bi-hourly (132 KV writes/day); D6-a ingestion interactive, scheduled part = discovery ALERT only; D7-a backfill from Oct-2025 peak; D7-c tab widget needs design context before ruling; v1 at Gate 6. ARCHITECTURE section 6 rewritten (tunnel now Phase 11); backlog skeletons P6-10.

2026-07-06 · gate-4 · bf2ae8f · Gate 4 CLOSED: PR #5 merged (phase-4 -> main). Owner ran rake deploy end-to-end (pre-flight, generated config, wrangler, real publish, 4-probe smoke green); site LIVE on the unguessable workers.dev host, noindex verified live via curl. Phase 5 (ops integration) elaborated on branch phase-5: M5-1 ops/ wrappers + prepared launchd plists, M5-2 tmux publish health line, M5-3 docs/RUNBOOK.md, M5-4 Gate 5 soak checklist; decision item D5-a (publish cadence, hourly recommended) awaits owner ruling.

2026-07-06 · M4-8b (owner feedback) · -- · "Banners still cut": the clipped element was the ECharts DATA TOOLTIP, not the meta bubble round 5 fixed -- confine:true confines to the chart CONTAINER (itself below the fold on the bottom row), and ECharts applies confine AFTER any position callback, clamping a flipped tooltip back down (caught by the new Playwright check, which failed first run). Renderer now applies a universal viewport-aware tooltip.position callback + confine:false to every chart (render-layer policy like the dark theme; payloads stay pure JSON). Owner challenge "how did you check?" was fair: round 5 tested only the element I changed -- new short-viewport test hovers REAL chart data via raw mouse.move and measures the z-9999999 tooltip DOM; 3/3 positions fully visible + bubble/table suite re-passed. Ruling + test recipe recorded in mimir-design.

2026-07-06 · M4-8 · -- · Live round 5 (fable): BTCo table moved inside its quadrant (chart 290px + table in one card; wide strip now only the no-chart fallback; render.js cards carry data-key so pages can target them) and bubbles flip upward at the viewport edge (orient() on open + .bubble.up in both pages -- generalizes confine:true to all floating UI). Both added to mimir-design as standing rulings. Playwright: down-open top row / flipped bottom row fully visible / in-quadrant table, 3/3; test gotcha logged: locator.hover() auto-scrolls and defeats scroll-position tests -- use mouse.move on measured coordinates.

2026-07-06 · M4-5i (owner ruling) · -- · Deploy means LIVE, data included: rake deploy now runs a real publish (PUBLISH_DRY_RUN=0) between wrangler deploy and the smoke probes, so a deploy never leaves the site rendering stale/chart-less data (the live Gate 4 state: KV still held the 7-key Gate 2 publish, dashboard chartless while the smoke PASSed on age alone). DEPLOY_SKIP_PUBLISH=1 for code-only pushes; cron owns routine publishing from Phase 5. Smoke index probe now checks CONTENT: all four chart:* keys must be listed or it FAILs. Also M4-5h en route: DEPLOY.md real-publish command corrected (unset PUBLISH_DRY_RUN means DRY) + the dashboard banners the no-chart-keys cause instead of a bare page. +4 tests (publish step/ordering/skip flag/chartless index).

2026-07-06 · M4-5g (live deploy failure) · -- · Owner run failed: "entry-point web/worker.mjs not found" -- wrangler resolves main/[assets] paths relative to the CONFIG FILE, and the generated config lived in data/, so it hunted data/web/worker.mjs (confirmed from the wrangler log, not guessed). Generated config moved to the REPO ROOT beside the template (gitignored by name); regression test pins co-location + that the template main/assets paths exist beside it; wrangler-failure tail widened 5->15 lines (the 5-line tail had cut off the actual [ERROR] line -- the symptom that reached the owner was the advice padding).

2026-07-06 · M4-5f (owner feedback) · -- · Tighter pre-flight: CLOUDFLARE_API_TOKEN presence joins the table (was unchecked -- auth failures surfaced only at wrangler time), and DEPLOY_NAME becomes REQUIRED with its value shown (D4-c: the worker name IS the public hostname; forgetting it silently deployed guessable "mimir" -- explicit choice enforced, format validated in the table). Real deploys abort on any FAIL row; dry run stays informational. +3 preflight tests.

2026-07-05 · M4-5e (owner ruling) · -- · ONE name too: the alias line (CLOUDFLARE_API_TOKEN="$CF_API_TOKEN") was rightly rejected as juggling -- CF_API_TOKEN/CF_ACCOUNT_ID/CF_KV_NAMESPACE_ID renamed to wrangler's canonical CLOUDFLARE_* everywhere (publisher kv_client, health probe, deploy, env redaction, tests, docs, .env.example, wrangler.toml placeholder). deploy.rb no longer maps the account id (wrangler reads the inherited env natively); its only child override unsets the RETIRED legacy CF_API_TOKEN name (blind rename had turned the strip into unsetting the real token -- caught in review, test now pins child env == {CF_API_TOKEN: nil}). Owner action: rename the three lines in ~/.config/mimir/env.

2026-07-05 · M4-5d (owner ruling) · -- · ONE token, both jobs. The Gate 2 least-privilege split (KV-only publisher token + separate wrangler OAuth) was owner-rejected at Gate 4 after it hijacked wrangler auth (wrangler adopts any CF_*/CLOUDFLARE_* env token over OAuth; login refuses outright while one is set) -- juggling two credentials for one box was the real cost. New setup: the existing token gets Workers Scripts Edit (+ User Details Read) added in the console (editing keeps the token value); ~/.config/mimir/env aliases CLOUDFLARE_API_TOKEN="$CF_API_TOKEN" so wrangler sees its modern name (no deprecation warning); publisher unchanged; the M4-5c child-env strip of the legacy name stays (wrangler sees only CLOUDFLARE_API_TOKEN). CLOUDFLARE_API_TOKEN joined SECRET_ENV redaction. DEPLOY.md 1.1 rewritten (no wrangler login anywhere), failure table updated, .env.example documents the ruling.

2026-07-05 · M4-5b (owner feedback) · -- · Gate 4 doc feedback ("DEPLOY.md mixes everything into a pile of confusion"): root cause was the two-surface architecture it documented (Worker + separate Pages with unresolved same-origin routing). Collapsed to ONE surface -- wrangler.toml [assets] serves web/ on the Worker's host (same-origin by construction, Pages step deleted; worker.mjs unchanged, asset paths never reach it); DEPLOY_NAME env optionally renames the worker (= unguessable hostname, ruling D4-c; validated, only the name line substituted); web/_headers carries X-Robots-Tag noindex; 4th smoke probe pins GET / = the dashboard shell. DEPLOY.md rewritten as a numbered runbook: do-this sections 1-3 (one-time setup / deploy / Gate 4 checklist with EXPECT lines), symptom->fix table (4), all background quarantined in section 5. ARCHITECTURE section-2 diagram + Phase 4 + Gate 4 amended; README updated. New tests: substitute_name (rename/noop/invalid), verdict_dashboard, 4-probe smoke; dashboard_note replaces the Pages reminder.

2026-07-05 · M4-5 · -- · Owner-run deploy automation (opus): lib/btc/deploy.rb -- pure, unit-testable pieces (substitute_template replaces ONLY the FILLED_FROM_CF_KV_NAMESPACE_ID_BY_RAKE_DEPLOY placeholder; env_rows say set/MISSING never the value; verdict_healthz/index/missing smoke judgments) + a thin orchestrator driven by an injected runner lambda(cmd,env_overrides)->[ok,out] and BTC::Http. Pipeline: pre-flight (wrangler --version, CF_ACCOUNT_ID/CF_KV_NAMESPACE_ID present, git tree clean, `rake` gate green -- last two skippable via DEPLOY_SKIP_CHECKS=1) -> generate data/wrangler.generated.toml (gitignored, verified via git check-ignore; namespace id from ENV) -> wrangler deploy -c that config (CLOUDFLARE_ACCOUNT_ID exported from CF_ACCOUNT_ID for the CHILD only, via Open3 env hash) -> smoke GET /healthz (200 {ok:true}), /api/v1/index (200 envelope, generated_at parses, age <7d), /api/v1/definitely:missing (404), PASS/FAIL per probe, nonzero exit on any FAIL. DEPLOY_DRY_RUN=1 does everything except the wrangler call + probes: prints the pre-flight table (informational/non-blocking in dry run so a wrangler-less/dirty dev box still verifies), the exact would-run command (CLOUDFLARE_ACCOUNT_ID shown as <from CF_ACCOUNT_ID>, never the value), and the Pages-is-a-separate-owner-step reminder -> docs/DEPLOY.md section 2. Secret discipline: no CF_* value ever printed (set/MISSING only; generated id line written not echoed; all external output through BTC::Env.redact). rake deploy REFUSES under CI (ENV['CI'] set -> abort quoting Golden Rule 3), NOT in the default gate, desc marks it owner-run/never-in-CI. Judgment calls: (1) pre-flight is non-blocking in DRY RUN only (real deploy fails fast) -- required so `DEPLOY_DRY_RUN=1 rake deploy` exits 0 on a box without wrangler / with a dirty tree, which is the CI-less verification path the task specifies; (2) full wrangler stdout is not dumped on success (only the parsed host + per-probe lines) to avoid leaking anything wrangler might echo -- failure prints a redacted 5-line tail. 21 unit tests (test/unit/test_btc_deploy.rb: substitution, MISSING-env shape w/ value-leak assertions, CI refusal, dry-run command assembly w/o execution via fake runner, smoke verdicts 200-ok/wrong-body/stale/404 via fake transport). docs/DEPLOY.md: first-time CF setup (workers.dev/route, unguessable Pages name + noindex per D4-c), routine rake deploy + pre-flight/smoke line meanings, Gate 4 smoke checklist (11 keys curl loop, 404 path, stale-badge behavior), rollback (wrangler rollback + re-deploy), AUTH_TOKEN activation (one wrangler secret put). README -> Phase 4 capability set (Worker API, dashboard w/ table + age tickers, rake preview both pages, rake deploy owner-run, SRI pin; not-yet list now ops/cron Phase 5 + queue-tail Access/LPPL price panel). Verified: rake green (298 runs / 12 node tests, 0 failures); DEPLOY_DRY_RUN=1 rake deploy exit 0 (dummies x/y absent from output, wrangler 4.106.0 present); CI=1 rake deploy exit 1 w/ Golden Rule 3; git check-ignore data/wrangler.generated.toml confirmed; ruby -c clean. web/, worker.mjs, publish/, goldens, fixtures, lib/btc/http.rb untouched.

2026-07-05 · M4-7 · -- · BTCo literal sortable table on web/index.html ONLY (opus): a 5th full-width card (.card.wide, grid-column 1 / -1) appended below the 2x2 charts, rendered from the SAME same-origin /api/v1/btco:latest suite envelope the bars chart consumes (raw payload, NOT a chart spec). One <table.btco> row per company; columns mirror btco.rb's --tmux line -- ticker (STALE red #e04b4b + placeholder * amber #e2a52b flags as their own spans, symbol stays normal), BTC held (thousands-sep int), mNAV/netNAV/leverage (2dp ratios, payload units unchanged -- no scaling, no invented numbers), btc_as_of. Numbers right-aligned, mono tabular-nums 11px; null -> dim '--' (GME netNAV). Card head: key-style title "btco:latest · universe" + the ticking age badge via the shared initBadge (data-generated-at/data-ttl scanned by the page's 1 s tickAll); NO bubble (the suite envelope carries no meta). Sort: header <button>s in <th> (.sortbtn reset, #e6a23c :focus-visible ring), click to sort / same header flips dir, aria-sort on the th, sorted header brightens (#d7dce3) vs dim (#7f8896); default BTC held desc (matches the chart); nulls always sort last (both dirs); pure tbody re-render from the kept array, no libraries, no pagination/scroll. Failure: producer-naming errCard ("btco:latest -- 404 ... Published by btco.rb"). Judgment call: leverage shown as raw 2dp ratio (0.31) per the task's "2dp for ratios", not btco.rb's --tmux 31% -- presentation only, no semantics touched; no new design ruling needed. Verified: node --check extracted inline script; rake green (277 runs / 12 node tests, 0 failures); headless screenshot READ (title + BTC-held header bright, others dim; 597,325 desc; STALE/* flags; GME netNAV dim --); Playwright/system-Chrome: default BTC desc, click mNAV -> desc(34.55..0.69) -> asc(0.69..34.55) with aria-sort desc/asc, sorted-header color rgb(215,220,227) vs dim rgb(127,136,150), netNAV nulls last in BOTH directions. preview.html/render.js/worker.mjs untouched.

2026-07-05 · M4-4 · -- · ECharts SRI pin (sonnet): integrity="sha384-pPi0zxBAoDu6+..." + crossorigin="anonymous" added to the ONE pinned CDN script tag in both web/preview.html and web/index.html (echarts@5.6.0, hash computed via curl|openssl|base64 at implementation time). Offline drift check: BTC::Health.scan_sri(pages) in lib/btc/health.rb (ECHARTS_PIN constant; flags missing integrity/crossorigin, hash mismatch, version or hash disagreement between pages); wired into rake health (reads both pages). 7 unit tests in test/unit/test_btc_health.rb (clean, real-files, missing-integrity, missing-crossorigin, wrong-hash, version-mismatch, hash-disagreement). Red-check verified: corrupting the hash in one page -> rake health exits 1 naming "web/preview.html: integrity hash mismatch" + cross-page disagreement. Browser check (Chrome headless --virtual-time-budget=8000): all four charts render on both pages (SRI hash accepted by browser). rake gate: 277 runs / 12 node tests, 0 failures.

2026-07-05 · M4-3 · -- · Production dashboard web/index.html (opus): same-origin loader over the shared render.js -- GET /api/v1/index for the key list, /api/v1/:key per chart, /healthz for the banner probe. ONE-line header (prose "mimir" + mono per-key chips dot+key@HH:MM + right slot "pub HH:MMZ · n/11 fresh", n = green keys over ALL published keys incl. the index envelope itself, so 11/11 all-fresh). Mono data face (ui-monospace/SF Mono, tabular-nums) on every number/status token; sans for titles/bubbles. THE SIGNATURE: live age ticker -- one shared 1 s setInterval rewrites each card badge's age (MmSSs <1h / Hh MMm <1d / Dd Hh) and re-evaluates its staleness dot from data-generated-at/data-ttl (page-owned; text-only so reduced-motion-safe). 2x2 grid in preview's order, 1-col <1100px. Failure copy: index-fail banner names /api/v1/index + the exact curl https://<host>/healthz; chart 404 errCard names the key + its producer (gex_profile<-gex_btc_combined.rb, scenario_strip<-scenario.rb, lppl_regime<-lppl.rb, btco_table<-btco.rb). A11y floor (render.js, both pages): gex_cp widget pieces are real <button type=button> (.cp reset + 2px #e6a23c :focus-visible ring), .info is tabindex=0 and opens the bubble on card-head :focus-within (was hover-only) -- preview.html renders/toggles identically. Offline review path: preview_server.rb shims GET /api/v1/:key -> data/publish_preview/<key : -> _>.json and GET /healthz (worker.mjs allowlist mirrored; regex forbids traversal), so `rake preview` serves the dashboard too. Verified: node --check render.js + both inline scripts; rake green (269 runs / 12 worker tests / +7 preview-server tests, 0 failures); headless screenshots (dashboard: 4 charts, 1-line header, mono chips, 11/11 fresh, (p) DERI (c) widget, ages ticking; preview unchanged); Playwright on system Chrome (age 3m14s->3m16s over 2s; (p) DERI click -> series selected=false + dimmed button; .cp/.info :focus + bubble block).
 -- · Shared renderer extracted (opus, behavior-preserving): staleClass/hhmm/buildBubble/errCard, the FORMATTERS (gex_levels) + WIDGETS (gex_cp) registries, and a new buildChartCard(env,key) (card DOM + head/bubble/badge + echarts.init 'dark' + verbatim setOption + the three meta hooks tooltip_formatter/height/legend_widget + charts.push) moved verbatim from preview.html into web/render.js, exposing ONE global window.MimirRender for preview.html AND the future index.html (M4-3). preview.html slimmed to a loader (local-file getJSON, index health strip, boot loop, resize) that <script src=render.js>'s it; all rulings comments (dark-theme rationale, hook sanctioning) moved with the code. Review fix (fable): buildChartCard self-resizes via requestAnimationFrame after the caller appends (echarts.init runs on the detached card), replacing the agent's fragile charts[last].resize() in the page -- M4-3 inherits the clean API. Verified: node --check both files; before/after headless preview screenshots BYTE-IDENTICAL (all four charts + strip + (p) DERI (c) widget); rake green (263 runs / 12 web tests, 0 failures).

2026-07-05 · M4-1 · -- · Worker API (fable: secret-adjacent + first-of-kind JS runtime/test contract): web/worker.mjs GET /api/v1/:key -> KV envelope VERBATIM (public max-age=60, X-Generated-At/X-Data-Age-Seconds from a fail-soft parse), strict key allowlist rejected before KV, JSON 404s, 405+Allow, /healthz no-store; AUTH_TOKEN bearer branch present but DORMANT (owner D4-c: Gate 4 public-read; constant-time compare, 401 never echoes). wrangler.toml is a TEMPLATE (namespace id filled from ENV by rake deploy, M4-5). Test harness: node built-in runner (zero npm), 12 tests w/ injected fake KV + frozen clock, incl. the a/../b URL-normalization pin; rake web:test joins the default gate + CI (WARN-skip without node).

2026-07-05 · gate-3 CLOSED · 68b0965 · Owner merged PR #4 after four visual review rounds (M3-8..11); goldens owner-blessed. Phase 4 opened on phase-4 (Cloudflare layer per ARCHITECTURE Phase 4): design pass with the frontend-design skill, then worker.js + dashboard packets M4-1.. elaborated under the revised tiering; Gate 4 deploy stays a HUMAN action.

2026-07-05 · M3-11 · -- · Review round 4 (fable: third renderer hook is a contract change): scenario/lppl y-axis names moved onto the axis (nameLocation middle, rotated -- top collided with the one-line titles, bottom would hit the time labels); GEX legend collapsed to one line per venue via NEW meta.legend_widget hook ('gex_cp': `(p) DERI (c)`, p/c toggle a side, name toggles both; spec ships legend.show=false so ECharts keeps selection state); GEX call/put columns overlay exactly (barGap -100%, safe with calls>=0/puts<=0). Self-review: headless screenshots + Playwright-vs-system-Chrome interaction run (5/5 toggle semantics incl. dim states); goldens re-blessed, still PROVISIONAL until Gate 3.

---
2026-07-05 · mimir-design skill · -- · Owner signed off: project-local .claude/skills/mimir-design/SKILL.md encoding the accumulated design rulings (compact/one-line titles, right-side panels, instant structured hover help, one-line-per-entity hover aggregation, significance filtering, filled sparse symbols, label collision rules, dark-theme discipline, palette anchors) + hard rendering constraints + the DEV-LOOP 6b self-screenshot protocol. Auto-triggers for any visual packet, mine or a subagent's. Marketplace skills installed by owner: example-skills bundle (frontend-design for the Phase 4 design pass, webapp-testing for interaction-state review).
2026-07-05 · M3-10 · -- · Review round 3 (fable): the grey-on-grey and inverted legend states were ECharts LIGHT-theme defaults on our dark page -- renderer now inits with the built-in dark theme (specs: backgroundColor transparent), page greys brightened, sparse ledger series get FILLED 7px dots (default emptyCircle ring invisible on dark -- found via headless-Chrome self-screenshot, minimal repro isolated it), lppl bound/floor labels split to opposite ends, panel grid clears the title. Loop practice codified in DEV-LOOP 6b: visual work self-screenshots and self-reviews before every owner handoff.
2026-07-05 · M3-9 · -- · Review round 2 (fable): renderer-hook contract introduced (meta.tooltip_formatter naming a registry function -- gex_levels renders one line per venue with green/red C/P and level-line totals; meta.height for the half-quadrant scenario card), instant structured CSS hover bubbles replace native title= tooltips, tooltip confine:true everywhere, and the venue filter upgraded from nonzero to VISIBLE-at-$M-precision (>=0.05M somewhere) -- DERI/IBIT/FBTC/GBTC survive, five dust venues gone from legend/series/hover completely.
2026-07-05 · M3-8 · -- · Compaction round applied to the rest (opus, fable spec + review): scenario module scoreboard now a narrow right-side heatmap column (names on its y-axis, band labels moved inside the plot), one-line 13px titles carrying the load-bearing number on all charts (regime+composite / verdict+composite / stress+band), tight grids, METHODOLOGY-compressed meta (desc/axes/help) registered for all four charts, preview.html compact header + one-line dot-chip health strip + hover bubbles (title=desc, i-affordance=axes+help). Goldens re-blessed (gex byte-identical). Ready for the owner's Gate 3 re-review.
2026-07-05 · M3-7 · -- · Gate 3 design feedback round (fable): additive envelope meta (desc/axes/help hover strings, chart envelopes only, sourced from the CHARTS registry, piped through the pipeline); gex_profile compaction -- zero-OI venues omitted entirely, values in $M, cross-venue C/P aggregate rows lead the hover bubble in side colors, right-side vertical legend of stacked C/P toggles per venue (DERI), slider dropped, one-line 13px title, tight grid. Goldens re-blessed (still provisional). M3-8 (opus) applies the pattern to the other three + preview hover bubbles.
2026-07-05 · preview-serve · -- · Owner ruled: no python in the flow. rake preview added -- ~50-line stdlib TCPServer static server (localhost-only bind, GET-only, percent-decode + path containment pinned by unit tests incl. encoded traversal), MIME map; preview.html footer/comment and README switched to rake preview. Live-verified: page + json 200, plain and encoded traversal 404.
2026-07-05 · gate-3 · -- · PR #4 opened (phase-3 -> main): M3-1..6 done, 11/11 dry-run verified, CI green. Owner: preview.html visual pass, rake golden:approve, merge. Decision items flagged: LPPL price-vs-trend panel (needs a published price series), BTCo literal table (needs Phase 4 HTML).
2026-07-05 · M3-6 · -- · web/preview.html (opus, fable spec + review): single self-contained page, pinned echarts@5.6.0 CDN tag, chart set derived from index.json, client-side staleness badges (ttl green/3x amber/red), setOption(payload) verbatim (harness never post-processes -- the spec is the contract), red failure cards. Review fix: documented serve command switched to python3 -m http.server (ruby -run -e httpd needs webrick, gone from stdlib on Ruby 3+). Phase 3 complete to Gate 3.
2026-07-05 · M3-5 · -- · Pipeline emits v1:chart:* (opus, fable spec + review): charts build from the run's own collected payloads via the CHARTS registry, ttl inherited as min across sources, source-skip cascades to the chart (and a builder crash skips only the chart), index lists charts automatically, PUB status contract now n/11 (pins updated same change). Verified 11/11 dry-run on the real data dir.
2026-07-05 · M3-2..4 · -- · Three chart specs (opus batch, fable spec + review): scenario_strip (composite time axis + regime-band markLines + 7-module heatmap w/ hidden piecewise visualMap), lppl_regime (three time-aligned panels: ratio vs bound/floor, log10 BF, Z; null ledger fields dropped; trough note only when the fit projects one), btco_table (BTC-desc labeled bars w/ STALE/placeholder flags, netNAV null gaps, NAV-parity markLine, stress gauge w/ band colors). Goldens blessed provisionally; gex golden byte-identical. Reviewer notes for Gate 3: btco x-axis outlier (GME mNAV 34.6 placeholder), lppl bound/floor overlap (both 0.434 in fixture).
2026-07-05 · M3-1 · -- · Chart-spec pattern set (fable): Publish::Charts with the CHARTS registry (golden test + golden:approve + future pipeline all iterate it), gex_profile (per-venue put/call stacks, flip/CW/PW markLines snapped to bins, +-30% default zoom window, deterministic venue shades), payload fixtures recorded from a live dry-run into test/fixtures/payloads/, golden harness (byte-diff, red = human question, never auto-blessed) with reworked deterministic rake golden:approve; additive gex_btc_combined profiles field landed separately. Provisional golden committed; Gate 3 re-blesses after preview.html review.
2026-07-05 · gate-2 CLOSED · 1565702 · Owner merged PR #3 after first real publish (PUB LIVE 7/7 keys; probe limit=5 bug fixed en route). Phase 3 opened on phase-3: M3-1 gex_profile + golden harness + payload fixtures (fable: first-of-family + additive gex output field), M3-2..4 remaining specs (opus), M3-5 chart keys in the pipeline (opus), M3-6 web/preview.html (opus, flagged web/).
2026-07-04 · gate-2 · -- · PR #3 opened (phase-2 -> main): M2-1..3 done under the revised tiering (fable: kv_client + specs + reviews; opus: envelope, orchestrator), CI green. Owner: review dry-run artifacts, create KV namespace + token, first real publish by hand, merge.
2026-07-04 · M2-3 · -- · Publish orchestrator (opus subagent, fable spec + review): producer table w/ per-key ttls, full-stdout JSON parse (aggregators print pretty json), skip-dont-publish-garbage semantics, 90d/365d history tails, dry-run-default preview writer, real-mode v1: KV PUTs abort-on-failure, new frozen PUB status line. Live dry-run artifact set generated and verified (envelope contract + sorted index). Phase 2 complete to Gate 2.
2026-07-04 · M2-1 + M2-2 · -- · Phase 2 opened: Publish::KV client (fable: bearer auth, 1s/2s/4s bounded backoff on 429/5xx/transport errors, immediate fail on other 4xx, token/body redaction pinned in every error path; BTC::Http gains put; env-gated cloudflare-kv probe registered; health conventions scan now covers publish/) and Publish.wrap/build_index envelope builders (opus subagent, fable spec + review, zero deviations; frozen envelope key-set pinned).
2026-07-04 · gate-1 CLOSED · c2b38e0 · Owner merged PR #2 (21 commits): fixtures + contract harness + F-20..23 fixes + fixtures:verify + revised tiering policy. Phase 2 opened on branch phase-2: M2-1 kv_client (fable: secret-adjacent first-of-family), M2-2 envelope/index (opus), M2-3 publish orchestrator (opus).
2026-07-04 · M1-16 · -- · rake fixtures:verify (owner-ruled: automation over eyeballing): per-fixture stat digests on the FIXTURES registry (spot/expiry ranges/row counts/last dates), README<->disk drift check, secret-leak scan, board-aging and stale-recording WARNs; joins the default gate and prints after every record run. FIRST PACKET DELEGATED under the new tiering: Fable wrote the spec and reviewed, Opus implemented (~70k subagent tokens), one review fix (record task still said eyeball-the-diff). Owner keyed re-record landed coinglass_flows.json -- contract suite now 0 skips.
2026-07-04 · tiering-policy · -- · Owner-ruled Gate 1 revision: Fable orchestrates/designs/reviews, Opus implements against specs, Sonnet does pattern-following with an oracle; fable tags on coding packets need a one-line justification; when in doubt tag cheaper (failed verification bumps one tier). DEV-LOOP sections 2/4/5 + backlog header updated; Phase 2 packets get elaborated under this policy at Gate 1.
2026-07-04 · M1-14/M1-15 (F-22/F-23) · -- · Owner-ruled: per-row flows parser (BTC::Flows, exact pins vs the archived real page -- old regex had reported day-of-month numbers as daily totals since inception) + three-leg source chain (farside -> archive.org web/2id_ via new BTC::Http.get_follow -> CoinGlass keyed). Additive 'source' field, recorder chain + coinglass fixture entry, health registry soft/follow probe support, SECRET_ENV + .env.example + README/METHODOLOGY history caveat. Farside fixture re-recorded from the 2026-07-04 archive snapshot; 1 contract skip remains until the owner's keyed record run.
2026-07-04 · gate-1 · -- · PR #2 opened (phase-1 -> main): M1-6..11 done, 50 contract tests green (5 self-gated skips await the fixtures re-record), CI green both OSes. Owner: re-run rake fixtures:record (F-20/21/22), confirm 0 skips, rule on F-22 analytics item, merge.
2026-07-04 · M1-11 · -- · Ingest contracts: pending-proposal JSON schema via a real heuristic --file run in a sandboxed copy of scripts/btco+lib (no ANTHROPIC_API_KEY, diff from/to entries pinned), --dry EDGAR discovery against the recorded submissions fixture (no state writes), strip_html/excerpt on the recorded 8-K within the 40k cap, and Golden Rule 5 tripwires: SCHEMA_NOTE + prompt fragments pinned verbatim against ingest.rb source, KEYRE/window/cap against ingest_text.rb; tripwire red-check verified by prompt mutation.
2026-07-04 · M1-10 · -- · btco contract tests on a synthetic 2-company universe (--universe): top-level stress/band/companies[] exact key sets, verdict vocabulary, STALE (>120d vs frozen clock) and placeholder flags, JPY manual_px + frankfurter FX path, --tmux line format incl. stale marker, and all three fail-soft shapes (missing universe / deribit down / nothing priced).
2026-07-04 · M1-9 · -- · LPPL suite contract tests on a deterministic synthetic cache (2014-2026 daily in a temp BTC_DATA_DIR: power law + 4y oscillation covering envelope's three trough dates, engineered 2026-03-01 peak, power-decay tail with log-periodic wiggle): all five tests' required/optional detail key sets, fail-soft without cache, aggregator composite/verdict/status_line/tests[], ledger.jsonl line field set, /tmp/lppl.status format. 13 tests, ~6s, fully offline (shim 404s any accidental fetch).
2026-07-04 · M1-8 + F-21/F-22 · -- · Scenario suite contract tests: per-module success + fail-soft shapes (exact key sets, F-9 one-line stdout, F-11 funding/onchain name quirks, F-12 unavailable marker), macro no-key path, aggregator composite/regime/modules and --tmux line. Found F-21 (binance spot fixture never registered -- cb_premium untestable) and F-22 (farside trim counted date strings but the parser regex swallows alternate rows -- fixture recorded 6 parseable rows; trim now counts with the module regex; production row-swallowing flagged as an analytics decision item).
2026-07-04 · F-20 + M1-7 · 63d36aa.. · Fixture board trims fixed to pick parser-live far-dated rows (pre-fix recording captured an expired zero-greek CBOE weekly board); owner re-record queued. Contract harness built: test/support/fake_transport.rb preload shim (fixture-backed transport via RUBYOPT, FAKE_NOW clock frozen to the provenance stamp so expiring fixtures never age out) + contract_helper with exact key-set pins. gex family covered: gex.rb/gex_us.rb both F-10 shapes/combined venues+combined+profile, --tmux line formats, abort-on-dead-source; red-check verified by key rename.

2026-07-04 · M1-6 · -- · BTC::Fixtures recorder behind rake fixtures:record: 17 registered shapes (gex venues, all scenario sources incl. 4 FRED series, coinmetrics, edgar submissions + dependent 8-K doc fetch), per-shape trims (slimmed CBOE rows, USDT+USDC-only stables, consistent EDGAR array slices, farside shortest-prefix HTML), provenance README with redacted URLs. Unit-tested against fake transport; recording stays owner-run.
2026-07-04 · gate-0 CLOSED · 13eea5b · Owner merged PR #1; prices eyeballed fine; legacy crontabs scrapped pending Phase 5 ops; FRED/EDGAR key setup deferred to owner discussion. Phase 1 opened on branch phase-1: M1-6 fixtures:record + M1-7..11 contract tests elaborated.
2026-07-04 · gate-0 · -- · PR #1 opened (phase-0 -> main, 41 commits): all Phase 0 packets done, gates green, capture review complete. Owner merges to close Gate 0; CLAUDE.md phase line updates post-merge.
2026-07-04 · M0-9 · -- · docs/METHODOLOGY.md (owner request): interpretation guide for all four tool families -- rationale, model assumptions, field-by-field output tables, status-line decodes, worked LPPL reading (incl. the trend-BF cache-density caveat explaining -61 vs -425 magnitudes), quick score reference. Linked from README.
2026-07-04 · F-17/F-18+/F-19 · 08f6727..130395a · F-16/F-17 signed off; btco repriced via CBOE + Frankfurter (6/7 live, manual_px for JP). Health framework built (owner request): offline conventions scan joins the default gate; rake health:sources probes 14 registered endpoints with shape validators + anti-drift markers. Its first run caught F-19 (community tier gated CapRealUSD -> MVRV module silently score-0 for unknown duration); fixed via CapMVRVCur + derived realized price, all sources green. Loop discipline added: owner verification asks must carry exact commands.
2026-07-04 · R-3/6/7/12 + captures · fcbcfb2..e2525e8 · Owner-ruled round-2 completion: inst_gex unification (R-12, s^2 -- approved analytics change, verified live to ppm), put_call_ratio, BTC::Format. lppl cache rebuilt: CryptoCompare key-gated upstream (F-16) -> prices.rb switched to Coin Metrics community (5,830 rows, idempotent). Gate 0 captures run + reviewed (criterion amended, no pre-import copies exist): caught + fixed kwargs-parse bug at two Http.get sites (F-18); Stooq quote API dead upstream (F-17, decision item); first LPPL verdict STRESSED with BF -61 flagged for owner eyeball. M0-3 README accepted -> done.
2026-07-04 · M0-4..M0-8 · d07c5b6..8be32be · Characterization complete: deep bs_gamma properties + parser edge pins (M0-4/5); Lppl.lomb + reciprocal_envelope extracted and pinned on synthetic tone/decay, tolerance policy set (M0-6); Btco.company_row extracted, every metrics branch pinned on synthetic universe incl. JPY listing (M0-7); Btco strip_html/excerpt/diff_against extracted with window-geometry and diff-semantics pins (M0-8). All Phase 0 packets done; Gate 0 awaits owner captures + README/memo acceptance + PR merge.
2026-07-04 · M0-3 (draft) · -- · README.md v1: per-tool capabilities/commands/ENV with honest maturity (btco placeholder data called out), shared conventions, explicit not-implemented list (publish/charts/dashboard/fixtures), dev pointers. Owner accepts at Gate 0. M0-2 marked done (open R-items are Gate 0 decision items).

2026-07-03 · R-1..R-10 (recommended) · 0341e3c..2ce01e0 · Round-2 fixes: BTC::Deribit venue helper (5x index fetch collapsed, R-8 get_json ambiguity dissolved, verified live), Options.sign single definition, BTC::Suite.run_module + Report.status, suite namespace rule (Scenario/Lppl/Btco -- kills the double module Common landmine). Optional R-3/6/7 and flagged R-11/12/13 await owner review.
2026-07-03 · F-6/F-7..F-12/F-13 · d225614..b416b82 · Owner approved: BTC::Env.redact wired through BTC::Report (secrets can't reach stdout); fail_soft JSON gains additive 'unavailable':true (F-12); F-7/9/10/11 conventions pinned in headers; BTC::Http seam with injectable transport + StatusError, all six HTTP helpers migrated (net/http gone from scripts/), verified offline fail-soft matrix + live fetches. M1-1 core delivered early; fixtures/contract tests remain for Phase 1.
2026-07-03 · F-8/F-14/F-15 · 9b22457..38d4e7a · Owner approved DRY extraction + data regularization: lib/btc/{options,report,util,env}.rb with exact-value/byte-format tests first; gex family and suites switched over (net -180 lines of duplication); BTC_DATA_DIR resolution everywhere (M1-12 early); scenario history migrated HOME -> scenario/data/history.jsonl (auto-migration verified offline). HTTP helpers deliberately left for M1-1 seam.
2026-07-03 · F-1..F-5 · 32c0607/7490b65/9bd5c5a · Owner approved fixes for memo findings F-1..F-5: btco convert-FX bug fixed (metrics.rb extraction + red-first JPY regression), btco fail-soft aligned to module contract, fit.rb history append gated on --history (flag passed through by lppl.rb), F-3 mcap-basic confirmed deliberate + documented, F-4 gex failure modes documented in headers (code change deferred to Phase 2 design). Findings F-6+ awaiting owner review.
2026-07-03 · M0-2 (draft) · -- · docs/TOOL-REVIEW.md: full read of all 22 scripts (~3k lines); 15 findings incl. one confirmed latent bug (btco convert FX math, rate^2 understatement), fit.rb ungated history write, btco fail-soft mismatch, 5x HTTP helper duplication; Phase 1 refactor list proposed. Awaiting owner review.
2026-07-03 · M0-1 · -- · Loop bootstrap on phase-0: .claude/settings.json permission profile (repo-sandbox allow, hard deny on wrangler/fixtures:record/main-push/secrets), GitHub Actions CI (compat+test, ubuntu + macos-arm64, Ruby 3.3), backlog with elaborated Phase 0 packets, this worklog. Push policy decided: loop pushes phase-N + opens PRs, main owner-merged.
